---
name: "/kit-graph-check"
id: "kit-graph-check"
description: "结构变更后编排 graph yaml check（CLI 真值）"
kit_command_id: kit-graph-check
---

结构变更后：编排跑 `npx spec-wave graph yaml check`（按需 compile/export）。
真值在 CLI；勿口头代过。POINTER：README graph 段 · 消费者仓 `docs/_tech_graph/`（若有）。
禁止无闸 `kit-30` / `kit-publish`。
