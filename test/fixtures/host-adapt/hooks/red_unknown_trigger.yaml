# hooks 红 fixture（OQ-1）：未知 trigger（pre-push）→ enum fail-closed 报红
version: "1"
schema_version: 2
command_sets:
  core: [verify]
  expanded: [graph-check]
hosts:
  - host_id: a
    surfaces:
      always_on: []
      skills: []
      commands: []
      hooks:
        mechanism: shell-hook
        triggers: [pre-push]
        command: "x"
