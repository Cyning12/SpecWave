# hooks 红 fixture（F-W1-10）：非 none 缺 triggers → resolved 完备性报红
version: "1"
schema_version: 2
command_sets:
  core: [verify]
  expanded: [graph-check]
hosts:
  - host_id: a
    surfaces:
      always_on: []
      skills: []
      commands: []
      hooks:
        mechanism: config-hook
        command: "x"
