# S2.1 探测树 fixture：schema_version 整数 2 → v2 解析路径 · 本阶段为入口桩 fail-closed
# （全新校验 + defaults/extends 归 W1 后续阶段 · 不静默放行）
# 断言点：host validate exit 2 · 输出含「解析未实现」（test/w1-schema-version-detect.test.ts）
schema_version: 2
version: "1"
hosts:
  - host_id: cursor
    surfaces:
      always_on: []
      skills: []
      commands: []
