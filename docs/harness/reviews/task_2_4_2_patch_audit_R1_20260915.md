# 审查文 · 2.4.2 patch task R1（R-1/R-2/R-3 三项修复 + bump）

> **hat**：20-task-audit（书面审查 · R1 · 独立上下文 · 非起草者）
> **日期**：2026-09-15
> **被审对象**：[`docs/tasks/active/task_2_4_2_patch.md`](../../tasks/active/task_2_4_2_patch.md)（slug `2-4-2-patch` · 无独立 SPEC 夹 · 模板照 2.4.1 patch）
> **对照基准**：[`.workbuddy/output/验收报告-SpecWave-2.4.1.md`](../../../.workbuddy/output/验收报告-SpecWave-2.4.1.md) §3.1/§3.2/§3.3 + §4 残余登记 + §6.1 三条 + §6.2 归 3.0 + §6.3 仅人 · [`docs/tasks/done/task_2_4_1_patch.md`](../../tasks/done/task_2_4_1_patch.md)（patch 链路模板）
> **审查方式**：只读通读 task + 报告 §0–§7 + 2.4.1 模板；**行号现值抽查 8 组**；**R-2 提议新闸全量模拟实测**（临时脚本 `/tmp/w242_r2_sim.mjs` · 未改仓文件）；**未改被审对象任何字节**。
> **dogfood 自证**：本审查文结论节按 R-2 提议新闸口径自检（实质内容 ≥20 · 无同句 不/未…通过 共现 · 无 NOT PASS 形态）——实测脚本输出见 §3 末。

---

## 1. 逐项核对结论

| 核对项 | 结论 | 证据 |
|--------|------|------|
| 范围严格限 §6.1 三条 | **PASS** | R-1（host validate 缺省基 → findGitRoot 上溯 + `outside_repo: true` 占位）· R-2（`not\s*pass` 补词 + 同句共现窗 `{0,12}`）· R-3（htmlARe 无引号属性三选一）+ bump 九件套，与报告 §6.1 表逐行对应；**R-4 顺带授权核实**：§6.1 R-2 行原文「顺带把 R-4 的窗口从 `{0,3}` 放宽为同句共现语义判」· task 背景（:49）与 R1/R2 均正确标注「§6.1 口径优先于 §4 登记」 |
| R-5/R-6/3.0 项未混入 | **PASS** | 非范围表逐条列入：R-5（换行形态归 3.0 · **「不得顺手修」双保险**——范围 R-2 对照口径 + F-P3-11 打回条款）· R-6（git 可诊断性归 3.0 W5 · 本波仅登记 shim/exit 69 处置口径）· pin-08 版本↔发布态绑定（归 3.0 W4）· NEW-4..12 · 发布四动作仅人（2.4.1 代跑授权不延续 · 与 §6.3 一致） |
| K 形态「维持 PASS 漏网」断言 | **PASS（专项核对）** | 范围 R-2 对照条：「K（`不\n通过` 换行）**维持 PASS 漏网**（R-5 已登记归 3.0 · 窗口排除 `\n` 即此口径 · 不得顺手修）」；验收 R-2-b：「K 维持 PASS 漏网 · **断言入测防顺手修**」——把漏网固化为测试断言，30 顺手修会红测，防线成立 |
| 验收机械可判 | **PASS** | 抽查深核：R-1-c「`grep -c 'printJson(process.cwd()' src/cli.ts src/cli-host.ts` 维持 0/0」纯 grep 计数可判（现值实测 0/0 · 2.4.1 已清零 · 本波维持断言防回潮）；R-2-a/b exit code + 形态枚举可判；R-2-c 阈值量化（>3 份措辞巧合 → 回退）可判；R-3 exit 2 + 四形态零回退 + 三捕获组取值测可判；bump/pins/四门条款与 2.4.1 同构 |
| failure_paths | **PASS** | F-P3-01~14 成表无空壳：闸拒（01）· 发布越权（02 · 四动作仅人口径更新）· `git add -A`（03）· R-2 误伤与回退（04 · 与验收 R-2-c 闭环）· 波及静默放过（05）· fixture 误绿（06 · 点名「无引号 fixture 误写成带引号」）· 捕获组漏改（07 · TEST-LOCK）· R-1 占位与 --target 冲突（08）· 四门红（09）· pin-10 设计红误判（10）· 扩范围顺手修（11）· RELEASING 双重敏感（12）· 忘 manifest rebuild（13）· git shim exit 69 先对照实验（14 · 环境警示条款新增 · 报告 §2.1 教训承接正确） |
| R0–R5 思考轮 | **PASS** | 控制表逐轮回填（R0 证据钉节号 + 行号复核声明 · R2 三方案各列采纳/弃选理由 · R4 可测性映射「负向 ×6 + 误伤阈值判」· R5 early_stop=no「待 20 审裁定」正确姿态）；residual_risks 四条真实（② 直言英文句号未排除的跨句误中风险由存量实测兜底——与本审 §3 实测自洽） |
| 人工闸表 | **PASS** | HG-AUDIT-R1=**pending** blocks 30 · 00 代签授权模式延续注记在案 · 签前 30 拒改码明写；HG-SPEC-SIGNOFF=N/A（无独立 SPEC 夹 · 2.3.1/2.4.1 先例）正确；HG-TASK-DRAFT blocks `20, 30` 与模板一致；HG-NEXT-PLAN 注明 2026-09-15 维护者直接下令「针对性开启 2.4.2」 |
| 2.4.1 模板对齐 | **PASS** | 元信息字段集（test_strategy/code_quality_bar/experience_capture/kpi_*）· 闸表结构 · bump 链路（手工 package.json · 设计红留痕 · RELEASING 双重敏感 · ACCEPTANCE 档 + spec 索引行 · perl 双模式教训承接）· 提交信息约定（fix/chore 分离 · 禁 git add -A · 不裹挟 .workbuddy）——逐项对齐；差异点（发布四动作仅人 · 环境警示头部）均有报告出处，属如实更新非漂移 |

## 2. 行号现值抽查（8 组 · 本棒只读实测）

| # | 引用 | 现值 | 结果 |
|---|------|------|------|
| 1 | `src/cli-checks.ts:686` REVIEW_NEG_RE（2.4.1 形态） | `:686` 逐字命中：`不.{0,3}通过\|未.{0,3}通过\|no\s*pass\|reject\|…`（i 旗标）· :681-682 注释含 R-5 换行残余留痕（2.4.1 R1 §3-1 裁决落码 ✓） | ✅ |
| 2 | `src/cli-shared.ts:37` findGitRoot | `:37` `export function findGitRoot(start: string): string \| null` 逐字命中 | ✅ |
| 3 | `src/cli-host.ts` cmdHostValidate `:463-545` · target 解析 `:484` | 函数起 `:463` ✓ · `:484` `const target = resolveTarget(process.cwd(), targetArg)` 逐字命中（缺省基=cwd 病根属实）· **函数实收 ~`:539`**（`:463-545` 区间 overshoot 6 行 · 提示级） | ⚠️ 提示级 |
| 4 | 三面 printJson `:495` / `:512` / `:529` | 三处 `printJson(target, { command: 'host validate', file: abs, …})` 逐字命中（基参=target=缺省 cwd · 与 R-1 定性一致） | ✅ |
| 5 | 人类输出 `toRel(process.cwd(), abs)` `:543` | 实在 **`:537`**（`console.log(\`file: ${toRel(process.cwd(), abs)}\`)` · 差 6 行 · 语义指向正确） | ⚠️ 提示级 |
| 6 | `src/cli-host.ts:781-799` emitHostFail 基参透传 | `:781` 起 · 注释「2.4.1 NEW-2：基参取命令 target（调用方透传）」· `printJson(base, …)` 透传面属实 | ✅ |
| 7 | `src/cli-pins.ts:325` htmlARe · `:336` 消费点 | `:325` `/<a\s[^>]*?href\s*=\s*["']([^"']+)["'][^>]*>/gi` 逐字命中（**只匹配带引号** · R-3 病根属实）· `:336` `found.push({ raw: m[1]!, … })` + 注释「捕获组 1 必参与（E5 收窄）」（F-P3-07 的联改对象确认在位） | ✅ |
| 8 | 基线数字（596/595+1skip · pins 17/17 · assets 110/110 · HEAD=3ef69ad · tag v2.4.1↔c89f92d） | 与报告 §1/§5 实测值逐字一致 · 「禁前置重跑全量 npm test」纪律与 2.4.1 同口径 | ✅ |

## 3. 重点审查：R-2-c 误伤实测条款 + 本棒预判模拟

### 3.1 条款本身（二分判定 · 阈值 · 回退路径）

- **二分判定口径**：① 原 PASS 改判 FAIL 且核因为**真实否定语义** → 不算误伤（闸变严正确拦截 · 循豁免先例处置）；② 核因为**措辞巧合** → 记误伤。二分标准写成明文可执行（核因动作 = 读命中片段判语义），且预留了「①类也须逐份留痕」的处置路径——可操作 ✓。
- **阈值**：措辞巧合误伤 **> 3 份 → 回退保守档**（`{0,3}` + 仅补 `not\s*pass`）。对 ~52–57 份 PASS 面的 ≈5–6% 容忍带，patch 级判据变更下合理 ✓。
- **回退路径**：保守档仍修 G 行（`NOT PASS` · `not\s*pass` 补词与窗宽解耦），仅 I/J（插 4 字）让渡归 3.0 W4——**回退后仍有净收益**，不是全有或全无；回退决策 + 实测数据双落盘（task 修订记录 + reviews/）——可操作 ✓。

### 3.2 本棒预判模拟（临时脚本 `/tmp/w242_r2_sim.mjs` · 全量直评 · 未改仓文件）

用**现行** `REVIEW_NEG_RE`（:686）与**提议新闸**（`不[^。；\n]{0,12}通过` / `未[^。；\n]{0,12}通过` / 补 `not\s*pass` · i 旗标 · 其余分支不动），套 `evalReviewConclusion` 全判定链（节抽取 → 否定守卫 → 通过词 → S1·N=20 内容量）直评：

| 面 | 总量 | 现行 PASS | 新闸翻转（OLD=PASS → NEW=FAIL） |
|----|------|-----------|--------------------------------|
| A · done task latest review（机读闸真实消费面） | 67 | 52 | **0** |
| B · `docs/harness/reviews/` 全件直评 | 76 | 57 | **0** |

**预判：措辞巧合误伤 = 0 · 真实否定语义翻转 = 0 —— >3 回退阈值几乎不可能触发，R-2-c 定档合理。** 30 阶段的硬条款实测应复现同一结论（若复现出翻转则说明本脚本与实现有语义差 · 本身即是有效信号）。

形态单测（新闸）：`NOT PASS` / `not pass` → 拦 ✓ · `不最终予以通过` / `未能够予以通过` → 拦 ✓（I/J 修复成立）· `不\n通过` → **不拦**（K 维持漏网 · 与 R-5 登记口径一致 ✓）· 既有 B/D/E/L/M 形态 → 仍拦 ✓ 零回退。**误伤窗理论存在**：「不再阻塞，予以通过」「不重复造轮子，通过验收」类同句共现会被拦（措辞巧合类真实存在）——但存量 52+57 份零命中；此窗由 R-2-c 实测 + F-P3-04 阈值兜底，且 20-task-audit 帽条文模板侧建议后续措辞避开「不…通过」同句共现（提示级 · 不阻塞）。

### 3.3 dogfood 自检（本审查文 vs R-2 提议新闸）

本审查文结论节（见文末）：含可机读通过词 · 去通过词后实质内容远超 20 字符 · 无 `不/未[^。；\n]{0,12}通过` 同句共现 · 无 `no/not\s*pass`/`reject` 形态 · 无未豁免「退回」——按 §3.2 同一脚本口径自检通过（攻击形态枚举全部收在正文 §3 节而非结论节 · 结论节保持干净）。

## 4. 发现的问题

- **无 FAIL 级 · 无内容级阻塞**。
- **提示级 ×1（行号漂移）**：task 必读列表引 cmdHostValidate 区间 `:463-545` 与人类输出 toRel `:543`——现值函数实收 ~`:539`、toRel 实在 `:537`（各差 6 行 · 语义指向均正确 · 30 以现值为准即可）。R-1 范围条的 `:495/:512/:529` 三面 printJson 精确命中，不受影响。

## 结论

**R1 总结论：PASS · 零内容阻塞 · 可进 00 代签 HG-AUDIT-R1 → 30 开工。**

范围严格限报告 §6.1 三条 P2（R-4 顺带有 §6.1 原文授权 · R-5 换行形态被 K 断言双保险钉死 · R-5/R-6/pin-08 绑定/NEW-4..12 均未混入）；验收机械可判（grep 计数 · exit code · 量化阈值）；failure_paths F-P3-01~14 成表且新增环境警示条款承接报告 §2.1 教训；R0–R5 在位；闸表正确（HG-AUDIT-R1 pending blocks 30 · 发布四动作仅人且 2.4.1 代跑授权不延续口径与 §6.3 逐字一致）；行号抽查 8 组中 7 组精确命中、1 组提示级漂移（§4）。R-2 误伤预判模拟：A 面 52 份 / B 面 57 份现行 PASS 件在新闸下翻转 **0**，回退阈值定档合理，回退路径（保守档仍修 G 行）可操作且有净收益。本审查文自身已过 R-2 提议新闸口径自检（dogfood）。

## 维护者签闸（20 后 · 30 前）

> 本 task HG-AUDIT-R1 由 **00 代签**（维护者授权模式延续 · task 人工闸表在案）：

- [ ] 已读 R1 审查结论（含 §3 R-2 预判数字与 §4 提示级漂移）
- [ ] 在 task 人工闸表将 HG-AUDIT-R1 改为 approved（00 代签 · 注明授权出处与日期 · **status 格恰为 `**approved**` · 注记落说明列**——2.4.0 闸格混格教训勿复发）
- [ ] commit task 文档（禁 `git add -A`）
- [ ] 再下发 Harness 30 Prompt

30 Agent 将以 task 表为准；pending 时必须拒开工（见 TEMPLATE_30_gate_stop.md）。

---

**签名**：20 审查棒（20-task-audit · R1）· 2026-09-15 · 独立上下文非起草者 · 仅书面审查未改被审对象任何字节。
