# invoke · 30+40 · 2.4.1 patch 实现与验收（NEW-1/NEW-2/NEW-3/NEW-9+N9 修复段 + bump）

> **hat_id**：`30` + `40` · **日期**：2026-09-14
> **task_slug**：`2-4-1-patch`
> **帽条文**：`assets/harness/prompts/30-implement.md` · `40-acceptance.md`

## 开工闸

GATE_VERIFY 实测：`node bin/specgate.js verify --target . --task docs/tasks/active/task_2_4_1_patch.md` → 闸表 HG-TASK-DRAFT / HG-AUDIT-R1 均 approved · 补落 00 invoke 后三件套齐 · `VERIFY: PASS` exit 0 → 30 开工合法。

## 动作（30）

1. **负向 fixture 先行**（test_strategy=required · 修复前全真红留证）：
   - NEW-1 ×5（B「不予通过」/ D「不 通过」/ E「NO PASS」+ Reject / close 同口径 / A·C·F 对照 · `test/cli-w4-gate-wiring.test.ts`）——B/D fixture 首版误含「未通过」字面污染红证据，改干净形态重证真红
   - NEW-2 对偶 ×6（cwd≠target task close / symlink 入参 / realpath 双侧归一 verify / host validate --target / task lint 异目录 / exit-1 信封 :1299 · `test/cli-json-no-abs-path.test.ts`）——修复前 dest/file/message 绝对前缀泄漏逐条复现（报告 §3 代理实验同构造）
   - NEW-3（`<a href="FOO.md">` 修复前 exit 0 · 报告对照实验复现）· NEW-9 负向 ×3 + 正向 + 加长版本号两侧（`test/pins-consistency.test.ts`）
2. **NEW-1**：`src/cli-checks.ts` REVIEW_NEG_RE → `不.{0,3}通过`/`未.{0,3}通过`/`no\s*pass`/`reject`（i · R1 §3-1 定稿）；判定顺序实读留证（否定先于通过 · 病根确在正则覆盖面）；换行形态采 `.` 口径 + 注释留痕（R1 裁决）
3. **NEW-2**：cli.ts（task lint / close×3 / exit-1 信封）+ cli-host.ts（validate×3 / emitHostFail · emitU01Degraded 透传）基参全改命令 target（lint/close = findGitRoot 上溯仓根 · 无 .git 回落 cwd）；`cli-shared.ts` relativizeOutputValue realpath 双侧归一（词法+realpath 双基 · 悬空回落最近现存祖先）；`host validate` 补 `--target`（additive · HOST_USAGE 同步）；grep `printJson(process.cwd()` src/cli.ts src/cli-host.ts → **0 命中**
4. **NEW-3**：`cli-pins.ts` pin-16 提取补 htmlARe（单/双引号同口径 · 同一归一/判定管线）+ yaml pin-16 semantics 三形态同步
5. **NEW-9/N9**：`cli-pins.ts` hitA → `(?<![0-9A-Za-z._-])X\.Y\.Z(?![0-9A-Za-z._-])` 边界正则（R1 §3-2 定稿）+ yaml pin-08 semantics 边界口径同步；存量回归零误伤（真实仓 pins check A 组测绿）
6. **NEW-1 存量波及抽验**：evalReviewConclusion 直评现行 76 份审查文修复前/后双跑 → pass/fail 名单逐字一致（57/19 · 零误伤零豁免 · F-P2-05 不适用）；本 task R1 审查文过新闸（dogfood）
7. **bump 2.4.1**：package.json 唯一手工点 + package-lock 同步 · CHANGELOG `## [2.4.1] - 2026-09-14` 先落盘（待发版口径）· `pins fix --yes` 对齐 9 落点（pin-10 设计红留痕 · pin-08 D-SPEC-213-ROW 人工补行）· 叙事漂移巡修改回真值 ×4（RELEASING:13 / README 双语 :375 published 指针→2.4.0 / MIGRATION:3 minor→patch）· 未钉现行版引用联改 README 双语 ×8 + 测试断言 8 文件（perl 双模式）· RELEASING 台账 ×2 + 人 checklist 2.4.1 节（含授权注记 · 改后全量 npm test 复跑 · F-P2-12）· ACCEPTANCE_2_4_1 落盘（三节式照 2.3.1）· spec 索引 2.4.1 patch 收尾行 · assets manifest rebuild（~4 变更）→ verify 110/110（F-P2-13 收口）

## 验收（40）

逐条真实命令自证见 task 自检结论节：NEW-1 负向/存量/dogfood ✅ · NEW-2 grep 0 命中 + 对偶 ×6 + host validate 接口面（补 --target 落地 · 无参零回退）✅ · NEW-3 负向/对照 ✅ · NEW-9 三负向 + 全量回归 ✅ · bump 九件套 ✅ · 四门全绿（typecheck 0 错 · npm test 596/593 pass/0 意外红/1 skip · build · test:lib 6/6）· pins 16/17（pin-10 tag-gated 设计红留痕）· assets 110/110 · 裸 verify PASS · gate-check exit 0。

## 未做（禁区）

- 未执行 git tag / push / npm publish / deprecate（tag+push 授权 00 代跑 · publish 仅人）· 未用 `--force` / `git add -A` / `--allow-*` 绕闸
- 未碰 3.0 项（NEW-4..12 / N5 一行未动）· 未碰 host-adapt schema · assets/ 仅 yaml semantics 声明文字（rebuild 收口）· `.workbuddy/` 档本体零触碰
- 临时抽验脚本 `scripts/tmp-review-snapshot.mjs` 用后已删（不入提交）

## 下一棒

commit `fix(2.4.1): 验收报告 NEW-1/2/3/9 修复 + bump`（逐路径显式 add）→ 交付 00 执行 `git tag v2.4.1` + push（维护者 2026-09-14 授权）→ publish 仅人 → 打 tag 后复跑 pins 17/17 + npm test 全绿（RELEASING 人 checklist 2.4.1 节）。
