# invoke · 30+40 · 2.3.1 patch 实现与验收（N1/N11/N13 修复段）

> **hat_id**：`30` + `40` · **日期**：2026-09-14
> **task_slug**：`2-3-1-patch`
> **帽条文**：`assets/harness/prompts/30-implement.md` · `40-acceptance.md`

## 开工闸

GATE_VERIFY 实测：`npx spec-wave verify --target . --task docs/tasks/active/task_2_3_1_patch.md` → 闸表 HG-TASK-DRAFT / HG-AUDIT-R1 均 approved · `VERIFY: PASS` exit 0 → 30 开工合法。

## 动作（30）

1. **负向 fixture 先行**（test_strategy=required）：N11 A2 ×2（verify --task / task close · 修复前真红 = 验收报告 §3.L A2 组复现）· N13 ×3（00 无引号 / "00" / 123 真红锁）· pack-hygiene 正/负向（正向首跑即红 = N1 真实复现：包内 README.md.bak 双语 + assets 2×.bak）· pins B4/B11 联改（.bak 成功后自动清理新语义）
2. **N11**：`src/cli-checks.ts` evalReviewConclusion 无结论/签收节 → 直接判未通过（禁回退全文 · 注释块同步改口径）
3. **N13**：同文件 loadLegacyGateExempt 四字段改显式类型判 `typeof x === 'string' && x.length > 0`；`legacy-gate-exempt.yaml` 头注释补 YAML 引号规范
4. **N1 六件套**：`.gitignore` + `*.bak` · `package.json#files` + `"!assets/**/*.bak"` · 新建 `scripts/check-pack-hygiene.mjs` 接 prepublishOnly 链尾（failClosed exit 2 · --ignore-scripts 防 prepare 递归）· `src/cli-pins.ts` 备份写盘成功后 unlink 自动清理（只清本次自写 · F-P2-08）· 删本机 7 个 .bak · pack dry-run 自证 188 files .bak=0
5. **N11 存量波及处置**（循 W4 先例）：裸 verify 实测缺口 8 → 抽验 9 份样本（波及侧 4 核因 + 近期侧 5 无误伤含 dogfood）→ 豁免清单补 8 条（四字段齐 · reason 点名 N11 接线波及）→ 复跑 PASS（豁免命中 18）

## 验收（40）

逐条真实命令自证见 task 自检结论表：N1 a–f 全 ✅ · N11 A2/存量/dogfood 全 ✅ · N13 ✅ · 四门全绿（typecheck 0 错 · npm test 540 pass/0 fail/1 skip · build · test:lib 6/6）· pins 17/17（bump 前）· assets 110/110。

## 未做（禁区）

- 未执行 git tag / push / npm publish / deprecate（仅人）· 未用 --force / --allow-* 绕闸 · 未碰 host-adapt schema
- assets/ 资产本体零改动（删的是 manifest 双侧排除的未跟踪 .bak · assets verify 110/110 实证无回归 · 无需 rebuild）
- 未扩范围修 2.4 项（N7/N8/N9/N12 等一行未碰）

## 下一棒

gate-check → task close --yes → commit `fix(2.3.1)`（逐路径 add）→ bump 2.3.1 链路（package.json 唯一手工点 → CHANGELOG → pins fix --yes → RELEASING 台账+人 checklist【改后全量 npm test】→ ACCEPTANCE_2_3_1 → spec 索引行）→ `chore(release): bump to 2.3.1`。
