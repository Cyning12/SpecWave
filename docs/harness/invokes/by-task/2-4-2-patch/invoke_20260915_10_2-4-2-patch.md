# invoke · 10-task · 2.4.2 patch task 起草（验收报告 §6.1 三条 P2 + bump 转可验收 task）

> **hat_id**：`10-task` · **日期**：2026-09-15  
> **task_slug**：`2-4-2-patch`  
> **帽条文**：`assets/harness/prompts/10-task-requirements.md`

## 授权来源

维护者 2026-09-15 拿到 2.4.1 对抗式验收报告（PASS-with-issues · 无 P1）后**直接下令「针对性开启 2.4.2」**（HG-NEXT-PLAN=approved 落 task 表）；00 代签模式延续（HG-TASK-DRAFT 已签 · **HG-AUDIT-R1=pending 待 20 审查文落盘后由 00 签**）。**发布边界收紧回默认**：报告 §6.3 明示 tag/push/publish/deprecate 维持**仅人**——2.4.1 的 tag+push 代跑系 2026-09-14 一次性书面授权 · **不自动延续**（已落 task 非范围与 F-P3-02）。**00 只委派不亲自实现**（harness-00-delegate-only 口径）。

## 动作

1. 通读 `.workbuddy/output/验收报告-SpecWave-2.4.1.md` 全文（§0 无 P1 · §2.1 环境对照实验（git shim exit 69 · 换可用 git 后 5 红变 0 红）· §3.1 行 G/I/J/K · §3.2 第 5/7 行 · §3.3 pin-16 末行 + pin-08 C 形态 · §4 残余登记（R-4 建议形态）· §6.1/§6.2/§6.3）。
2. 起草 `docs/tasks/active/task_2_4_2_patch.md`（模板照 `docs/tasks/done/task_2_4_1_patch.md` · slug `2-4-2-patch` · 无独立 SPEC 夹 · HG-SPEC-SIGNOFF=N/A）。
3. 范围三条（§6.1 逐字承接）：R-1 host validate 缺省基改 `--file` 所在仓根 findGitRoot 上溯 + 仓外文件 JSON 标 `outside_repo: true`（`src/cli-host.ts:463-545` 三面 :495/:512/:529 + 人类输出 :543 · `src/cli-shared.ts:37` findGitRoot）· R-2 否定词表补 `not\s*pass` + `不/未` 窗口放宽同句共现 `[^。；\n]{0,12}`（`src/cli-checks.ts:686` · §6.1 口径优先于 §4 登记 · 排除 `\n` 维持 R-5 残余口径）· R-3 htmlARe 无引号属性值三选一（`src/cli-pins.ts:325` · 捕获组变三组 · 消费点 :336 联改）+ bump 2.4.2 九件套。
4. **行号全量亲自复核现值**（grep/sed 实证 · 与报告附 B 索引对照吻合）。
5. **R-2 误伤实测条款落法**（维护者明令）：验收 R-2-c 硬条款——`evalReviewConclusion` 直评 `docs/tasks/done/` 全量 + `docs/harness/reviews/` 现行件；判定二分（真实否定语义命中 = 闸变严正确拦截 · 逐份核因留痕循豁免先例 / 措辞巧合 = 误伤）；**阈值：措辞巧合误伤 > 3 份 → 回退保守档**（`{0,3}` + 仅补 `not\s*pass` · 同句共现归 3.0 W4 · 回退决策+实测数据入 task 修订记录与 reviews/）；≤ 3 份逐份处置留痕。F-P3-04 同步落 failure_paths。
6. 非范围显式列：R-5（换行形态 · 已登记 · K 断言钉死不得顺手修）· R-6（git 可诊断性 → 3.0 W5）· pin-08 版本↔发布态绑定（→ 3.0 W4）· NEW-4/5/6/7/8/10/11/12 · 发布四动作仅人。
7. 环境坑警示落 task 头部 + F-P3-14（git 调用 69/异常 → `PATH="/opt/homebrew/bin:$PATH"` 前置重跑 · 先对照实验再定性）。
8. `node bin/specgate.js task lint --file docs/tasks/active/task_2_4_2_patch.md` → 结果见 00 汇报（本 note 起草后实跑）。

## 未做（禁区）

- 未改 `src/` / `test/` / `assets/` / `package.json`（30 的事 · 且 HG-AUDIT-R1=pending）
- 未签发 HG-AUDIT-R1（待 20-task-audit R1 审查文落盘后由 00 代签）
- 未执行 git commit / tag / push / publish（commit 由 30 棒逐路径精确 add · 四动作仅人）
- 未碰 `.workbuddy/` 档本体 · 未碰 3.0 项（R-5/R-6/NEW-4..12/pin-08 语义绑定）

## 下一棒

20-task-audit R1 书面审（重点复核：R-2 窗宽 12 与排除字符集对英文审查文的边界 · 误伤阈值 3 份的合理性 · R-1 仓外占位口径的值级变更定性 · R-3 捕获组联改）→ 审查文落盘 `docs/harness/reviews/`（**dogfood：审查文自身须过 R-2 新闸**）→ **签 HG-AUDIT-R1=approved**（00 代签 · 维护者授权模式延续）→ 30/40（`fix(2.4.2): …` + `chore(release): bump to 2.4.2` · 禁 git add -A）。30 开工前须 `npx spec-wave verify --target . --task docs/tasks/active/task_2_4_2_patch.md` 过闸扫描。
