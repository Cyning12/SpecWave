# invoke · 30+40 · 2.4.2 patch 实现与验收（R-1/R-2/R-3 修复段 + bump）

> **hat_id**：`30` + `40` · **日期**：2026-09-15
> **task_slug**：`2-4-2-patch`
> **帽条文**：`assets/harness/prompts/30-implement.md` · `40-acceptance.md`

## 开工闸

GATE_VERIFY 实测：`node bin/specgate.js verify --target . --task docs/tasks/active/task_2_4_2_patch.md` → 闸表 HG-TASK-DRAFT / HG-AUDIT-R1 均 approved · 补落 00 invoke 后三件套齐 · `VERIFY: PASS` exit 0 → 30 开工合法。

## 动作（30）

1. **负向 fixture 先行**（test_strategy=required · 修复前全真红留证）：
   - R-1 ×3（cwd=/tmp 靶场缺省调用 file 绝对泄漏 / realpath 同型 / 仓外文件缺 outside_repo 标 · `test/cli-json-no-abs-path.test.ts`）+ R-1-c 零回退对照（--target 显式 + 缺省 cwd=仓根 + 人类输出）
   - R-2 ×3（NOT PASS / 不最终予以通过 / 未能够予以通过 + close 同口径 · `test/cli-w4-gate-wiring.test.ts`）+ 对照零回退（A/B/D/E/L/F/M）+ **K 换行形态维持 PASS 漏网断言**（防顺手修 R-5）
   - R-3 ×2（无引号负向 + 四形态零回退 & 三捕获组取值 · `test/pins-consistency.test.ts`）
2. **R-1**：`src/cli-host.ts` cmdHostValidate 三面 printJson + 人类输出基参改 `--file` 所在仓根（findGitRoot 上溯 · `--target` 显式优先不动）；仓外文件 JSON 标 `outside_repo: true` + file basename 占位（键集只增）；grep `printJson(process.cwd()` 维持 0/0
3. **R-2**：`src/cli-checks.ts` REVIEW_NEG_RE 补 `not\s*pass` + 窗口放宽同句共现 `不[^。；\n]{0,12}通过` / `未[^。；\n]{0,12}通过`（排除 `。；\n`）；**存量误伤实测（R-2-c 硬条款）**：evalReviewConclusion 双跑快照逐字 diff —— A 面 done 全量最新审查文 67 份（72 总量 5 无审查文）+ B 面 reviews 77 件 → IDENTICAL 零翻转（误伤 0 · 语义翻转 0 · 远低于 >3 回退阈值 · 无需回退）；临时脚本用后已删
4. **R-3**：`src/cli-pins.ts` htmlARe 三选一 `"([^"]+)"|'([^']+)'|([^\s>]+)` + 消费点 `m[1] ?? m[2] ?? m[3]` 联改（E5 注释同步）+ yaml pin-16 semantics 含无引号形态声明
5. **bump 2.4.2**：package.json 唯一手工点 + lock 同步 · CHANGELOG 节先落（待发版 + 全仅人边界）· pins fix --yes 9 落点（pin-10 设计红留痕）· 叙事漂移改回真值 ×4 · README 未钉引用 ×8 + 断言联改 8 文件（perl 双模式）· RELEASING 台账×2 + 人 checklist 2.4.2 节（原子推教训保持 · 改后全量 npm test）· ACCEPTANCE_2_4_2 三节式 · spec 索引 2.4.2 行 · assets manifest rebuild（~4 变更）→ verify 110/110

## 验收（40）

逐条真实命令自证见 task 自检结论节：R-1-a/b/c ✅ · R-2-a/b/c + dogfood ✅ · R-3 负向/四形态/三捕获组 ✅ · bump 九件套 ✅ · 四门全绿（typecheck 0 错 · npm test 607/604 pass/0 意外红/1 skip · build · test:lib 6/6）· pins 16/17（pin-10 设计红留痕）· assets 110/110 · 裸 verify PASS · gate-check exit 0。

## 未做（禁区）

- 未执行 git tag / push / npm publish / npm deprecate（**本版全仅人** · 2.4.1 代跑授权不延续）· 未用 `--force` / `git add -A` / `--allow-*`
- 未顺手修 R-5（K 断言钉死）· 未碰 R-6/pin-08 绑定/NEW-4..12（归 3.0）· 未碰 host-adapt schema · assets/ 仅 pin-16 semantics 声明文字 · `.workbuddy/` 与 PLAN_3_0 等域外档未裹挟
- git shim exit 69 本波未复发（F-P3-14 对照实验条款在位未触发）

## 下一棒

commit `fix(2.4.2): 验收报告 R-1/R-2/R-3 修复 + bump`（逐路径显式 add）→ 交付维护者执行 tag/push/publish（全仅人 · 原子推或 tag 先行）→ 打 tag 后复跑 pins 17/17 + npm test 全绿（RELEASING 人 checklist 2.4.2 节）。
