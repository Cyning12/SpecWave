# invoke · 00 · 2.2.1 patch 派工留档（pre-30）

> **hat_id**：`00` · **日期**：2026-09-12  
> **task_slug**：`2-2-1-patch`

## 人授权（原文意图）

维护者 2026-09-12 会话预授权：按验收报告（`.workbuddy/output/验收报告-SpecWave-2.2.0.md`）§7 节奏建议，P0（symlink）+ P1（pins fix）合入 2.2.1，`.gitignore` 与 `package.json#files` 加 GLOSSARY.md 两行级 P2 随行；发布本体 push/tag/npm publish 归维护者，Agent 禁止 → HG-TASK-DRAFT = **approved**（00 代签落表 · 与 2.2.0 系列代签先例同构）；**HG-AUDIT-R1 保持 pending**（待 20-task-audit R1 落盘后人签 · 00 不代签此闸）。

## 动作

1. 委派 10-task 起草 `docs/tasks/active/task_2_2_1_patch.md`（范围 5 项 = 4 修复 + bump · 非范围归 2.3 清单显式列出）
2. 落 task 人工闸表：HG-TASK-DRAFT=approved（00 代签注明授权出处）· HG-AUDIT-R1=pending
3. pre-30 invoke 留档：`invoke_20260912_{10,00}_2-2-1-patch.md`（20 审查 invoke 由 20-task-audit 落盘后补齐，三件套齐方可派 30 · v2.14+ 硬闸）
4. 禁令复述入 task failure_paths 与提交约定：禁 publish/tag/push/deprecate（仅人）· 禁 `git add -A`（`.gitignore` 生效前高危窗）· 不裹挟 `.workbuddy/` / `.bak`
5. 提交约定：`fix(2.2.1): ...`（修复）+ `chore(release): bump to 2.2.1`（bump 独立提交）

## 下一棒

20-task-audit R1 书面审（对照验收报告 §4 #1/#2/#3/#6 核对范围/非范围/验收/failure_paths/思考轮）→ 审查文落盘 → 维护者人签 HG-AUDIT-R1 → 派 30。30 开工前须 `npx spec-wave verify --target . --task docs/tasks/active/task_2_2_1_patch.md` 过闸扫描（HG-AUDIT-R1=pending 时 verify 机械拦 exit 2 · 30 拒改码）。
