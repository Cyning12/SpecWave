# invoke · 10-task · 2.2.1 patch task 起草（验收报告 PASS-with-issues 四项修复 + bump）

> **hat_id**：`10-task` · **日期**：2026-09-12  
> **task_slug**：`2-2-1-patch`  
> **帽条文**：`assets/harness/prompts/10-task-requirements.md`

## 授权来源

维护者 2026-09-12 会话预授权（验收报告 §7 节奏建议「P0+P1 合入 2.2.1 · .gitignore 与 GLOSSARY.md 随行」）· HG-TASK-DRAFT = approved（00 代签落表）；**HG-AUDIT-R1 = pending**（待 20-task-audit R1 审查文 + 人签 · 本帽不代签）。

## 动作

1. 起草 `docs/tasks/active/task_2_2_1_patch.md`（2.2.0 → 2.2.1 patch · 4 修复 + bump）
2. 预填 Harness 元信息：test_strategy=required / required_invoke_hats=`10,20,30,40,00` / git_branch=main / graph_delta=none / **wiki_delta=none（预填）** / invoke_retention_profile=default / close_pr_policy=exempt
3. 范围 5 项：#1 P0 symlink realpath 归卡（`src/cli-shared.ts` resolveTaskPath · 四调用点 · 悬空保持「未找到」· 负向测试）；#2 P1 pins fix 按文件聚合（`src/cli-pins.ts` planFix + 写盘循环 · 同文件双钉面测试）；#3 `.gitignore` 加 `.workbuddy/`；#6 `package.json#files` 加 `GLOSSARY.md`；bump 2.2.1（package.json 唯一手工点 → pins fix --yes → CHANGELOG `## [2.2.1] - 2026-09-12`）
4. 非范围显式列 2.3 清单：报告 §4 #4/#5/#7/#8/#9–#12 及 P3（#13/#14）· tag/push/publish 仅人 · `.workbuddy/` 档本体不碰
5. 验收标准全部机械可断言：symlink D 行场景四调用点被拒且 exit 非 0 · pins 同文件双钉面一次 fix 收敛且 check 转绿 · `git check-ignore .workbuddy` 命中 · tarball dry-run 含 GLOSSARY.md · 四门绿 + pins 12/12（pin-10 设计红口径同 W8）
6. failure_paths 11 行 + R0–R5 思考轮槽 + 控制表（residual_risks ×3：pin-10 tag-gated 设计红 · realpath 双侧归一 · S2 拒写时序）
7. `npx spec-wave task lint --file docs/tasks/active/task_2_2_1_patch.md` → PASS（真实输出贴 00 汇报）

## 未做（禁区）

- 未改 `src/` / `test/` / `package.json` / `.gitignore` / `CHANGELOG.md`（30 的事 · 且 HG-AUDIT-R1=pending）
- 未签发 HG-AUDIT-R1（仅人签）
- 未执行 git tag / git push / npm publish（仅人）
- 未读未碰 `.workbuddy/` 档本体内容（仅引用报告结论路径）

## 下一棒

20-task-audit R1 书面审 → 审查文落盘 → **人签 HG-AUDIT-R1=approved** → 00 派 30（`fix(2.2.1): ...` + `chore(release): bump to 2.2.1` · 禁 git add -A · 禁 tag/push/publish）。30 开工前须 `npx spec-wave verify --target . --task docs/tasks/active/task_2_2_1_patch.md` 过闸扫描。
