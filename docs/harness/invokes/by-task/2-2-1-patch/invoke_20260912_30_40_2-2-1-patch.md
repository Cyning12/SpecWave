# invoke · 30-execute-code + 40-self-check · 2.2.1 patch（验收报告 PASS-with-issues 四项修复 + bump）

> **hat_id**：`30-execute-code` + `40-self-check`（同 Agent 闭环 · DSH 子代理执行） · **日期**：2026-09-12  
> **task_slug**：`2-2-1-patch`  
> **帽条文**：`assets/harness/prompts/30-execute-code.md` · `40-self-check.md`

## 闸扫描（GATE_VERIFY · 首输出 · 读 task 人工闸表非 invoke 字面）

| human_gate_id | task表status | 用户/invoke声称 | 一致？ | blocks_30 | 30可开工？ |
|---------------|--------------|-----------------|--------|-----------|------------|
| HG-TASK-DRAFT | approved | approved | Y | Y(20,30) | — |
| HG-AUDIT-R1 | approved | approved | Y | Y(30) | ✅ |

- reviews：`task_2_2_1_patch_audit_R1_20260912.md` 存在且 R1 pass 零阻塞 · 是
- pre-30 invoke：required ∩ {10,20,00} = {10,20,00} 三件齐全 · 是
- 机械辅助：`npx spec-wave verify --target . --task docs/tasks/active/task_2_2_1_patch.md` → **VERIFY: PASS（exit 0）**

## 动作

1. **红先行（test_strategy=required）**：先落 5 个新测试再改实现——symlink 负向 ×4（四调用点穿透拒 / 单元拒 / 悬空「未找到」/ 仓内放行）于 `test/cli-security-closure.test.ts` + pins 同文件双钉面一次收敛 B11 于 `test/pins-consistency.test.ts`。修复前实测**真红**：symlink 三例真穿透 / B11 复现「pin-12 后写覆盖 pin-11 修复 = 静默部分修复」原病。
2. **修 #1（P0）**：`src/cli-shared.ts` `resolveTaskPath` 单点收口内叠加 **realpath 双侧归卡**（target 与 abs 各自 realpath 后再 `path.relative` 判 containment）；悬空/不存在路径回落「realpath 最近现存祖先 + 拼余量」（修掉 macOS `/tmp`→`/private/tmp` 单侧归一误拒）；返回值保持词法 `abs`（C3 输出口径零漂移）。四调用点自动同口径，未逐点补丁。
3. **修 #2（P1）**：`src/cli-pins.ts` `planFix` 增可选 `baseContent`（累计基准）· `cmdPinsFix` 按文件维护 cumulative 内容依序 plan · 写盘循环同文件只写最终内容一次、只备份一次。S2 先判后写 / dry-run / .bak / unfixable 语义不变（B3/B4/B5/B9 回归全绿）。
4. **修 #3/#4**：`.gitignore` + `.workbuddy/`；`package.json#files` + `GLOSSARY.md`（`npm pack --dry-run` 实测 183 文件含 GLOSSARY.md）。
5. **bump 2.2.1**：package.json version 唯一手工点 → `pins fix --yes` 写 7 钉面（pin-11/12 同文件**一次写盘收敛**——新聚合逻辑生产实证）→ 叙事漂移改回真值 ×3（README 双语现行包行 + RELEASING :13 由「2.2.1 已 published」假叙事改回「bump 已落 · 待发版 · registry latest 仍 2.2.0」）→ pins 未钉引用联改（README 迁移节 ×4+×4 · MIGRATION ×5 · AGENTS dogfood 行 · host-adapt kit_semver）→ 测试断言联改 8 文件（含正则转义形态 `2\.2\.0` 一轮 perl 全覆盖）→ docs/spec/README.md 补 `2.2.1`（patch 收尾行 · D-SPEC-213-ROW 先例）→ CHANGELOG `## [2.2.1] - 2026-09-12`（Fixed 四项 · 待发版口径）。
6. **验收自证（真实命令 + 输出见 task 自检结论表）**：symlink 四调用点拒（exit 1 · 哨兵零泄漏）· /tmp 靶场仓内路径 PASS · 悬空 exit 2「文件不存在」语义保持 · pins 双钉面实战一次收敛（[written] 一次 · 单 .bak）· 四门绿 · gate-check PASS。

## 停点

40 自检完成 → task close。**未执行**：git tag · git push · npm publish/deprecate（仅人 · 禁令守住）。wiki_delta=none（与 task 元信息一致；「词法归卡须 realpath」「pins fix 按文件聚合」两条教训留本经验总结，晋升与否归 20/00 裁定）。

## 已知边界（留痕 · 非阻塞）

- **pin-10 设计红 ×3 处**：`pins check`（11/12 · 唯一偏差 pin-10）、`test/release-tag-identity.test.ts`、`pins-consistency A组`——均为 tag-gated「git tag v2.2.1 缺失」，维护者打 tag 后复跑须全绿（W8 同构口径）。
- `.bak` 备份 ×6 留本机回滚（未入库 · F-P1-10 守住）。
- `.workbuddy/output/` 有 9 个**已跟踪**历史档（commit 2ef3a9d 架构材料）——.gitignore 只护未跟踪面；`git check-ignore .workbuddy` 裸目录因含已跟踪子档 exit 1，未跟踪成员（如 `.workbuddy/.DS_Store`）实测命中 `.gitignore:4` exit 0。已跟踪档是否下架归维护者裁定（本 task 非范围）。
- 同文件多钉面**模式重叠**（前序 fix 覆盖后序钉面全部 span）时后序 pin 会落 unfixable 误报——现行 12 钉面无此形态（pin-11/12 模式不相交），记候选债项归 2.3。
