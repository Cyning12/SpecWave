# SpecWave 3.0.0 使用手册

> **版本**：`spec-wave@3.0.2`（**已发布** · registry `latest=3.0.2` · `time.3.0.2`=2026-09-24T00:55:45.386Z · tag `v3.0.2` ↔ tip `3d71b90` · 30 代核回填 2026-09-24）
> **适用包**：`spec-wave@3.0.2`（已发布）
> **手册基线**：文件名与架构面仍为 3.0.0；3.0.1 / 3.0.2 patch 差异见 CHANGELOG `[3.0.1]` / `[3.0.2]` / MIGRATION「无强制动作项」
> **适用**：Cursor · Claude Code · DSH · CI 流水线 · 任一支持 Markdown 约定文件的编码 Agent
> **配套**：`README.md` / `README.zh-CN.md`（总览）· `GLOSSARY.md`（术语）· `MIGRATION.md`（迁移）· `RELEASING.md`（维护者）
> **本手册性质**：面向**使用者**的操作手册。所有命令均在本机真跑验证（macOS · Node v22.22.2）。
>
> **📌 占位说明**：本手册所有 `〔截图占位〕` / `〔录屏占位〕` 块是**待补的媒体素材**，块内已写明"画面内容 + 验证点 + 口播要点"，后续可直接按描述录制/截图后替换。编号可全文检索。

---

## 目录

| 章 | 标题 | 你会拿到什么 |
|----|------|-------------|
| 1 | 这是什么 | 30 秒建立正确预期（含**不该期待**什么） |
| 2 | 安装与版本确认 | 一条命令确认装的是 3.0.2（已发布） |
| 3 | 五分钟上手 | 三条最短路径（Cursor / Claude Code / CI） |
| 4 | 核心对象 | `task.md` · `spec.md` · 帽制 · 人工闸 |
| 5 | 初始化项目骨架 | `init` · `sync prompts` · 目录布局 |
| 6 | 多宿主物化 | `host validate/apply/update/verify/catalog` |
| 7 | 门禁链路与 hooks | 三层闸（L1/L2/L3）· `hook-guard` · 降级语义 |
| 8 | 校验与门禁命令 | `check` · `verify` · `gate-check` · `audit` · `status` |
| 9 | 任务生命周期 | `task lint` · `task close` · `timeline` · `lifecycle` |
| 10 | 图谱与本体 | `graph yaml/graph hgm` · `wiki` · 本体边界 |
| 11 | 审计与可观测 | `audit` · `discipline` · `pins` · `assets` |
| 12 | DSH 插件面 | 插件安装 · `apply_coding_standards` |
| 13 | 升级与迁移 | 2.x → 3.0.0（**含 v1 表零改动保证**） |
| 14 | 排障 FAQ | 10 个高频问题 |
| 附 A | 命令全表 | 一页速查 |
| 附 B | 截图 / 录屏占位清单 | 待补素材一览 |

---

## 1. 这是什么

**SpecWave** 是一个**多宿主编码 CLI**：用**单一声明式适配表**把「规范入口」物化到 13 个宿主（Cursor · Claude Code · 可选 DSH · agents · Copilot · Codex · Windsurf · Gemini CLI · opencode · Roo Code · Zed · Cline · aider）的原生落点，并提供一套**机械门禁**与 **Harness 过程命令**。

一句话记住它的定位：

> **闸的真值是终端 CLI 的退出码，不是聊天里说"我觉得通过了"。**

### 1.1 三个核心承诺

| 承诺 | 含义 |
|------|------|
| **门禁随包内置** | `check` / `verify` / `gate-check` / `audit` 的判定在 **CLI 进程内**完成，**不依赖**宿主是否真的执行了 hook |
| **failClosed** | P0 门禁失败一律 **exit 2**；CI 与 Agent 必须把 2 当**硬停** |
| **零行为变更的拆分** | 3.0.0 对 `src/` 做了大重构（god-file 拆分），但**对外可达面不变**（`exports` 只开 3 个入口，`lib/*` 子路径全部封死） |

### 1.2 ⚠️ 不该期待什么（诚实边界）

以下**尚未实现**，请勿对外声称（口径真值源：`assets/harness/claims-boundary.yaml`，机检 `scripts/check-claims.mjs`）：

| 不要声称 | 正确说法 |
|---------|---------|
| 「接入即获得 L3」 | 无 hook 机制的宿主**显式降级 L1+L2** |
| 「13 宿主全部有 hook 拦截」 | **config-hook 3 宿主**；shell-hook = git 层宿主中立；其余显式降级 |
| 「门禁靠宿主 hook 强制」 | 门禁靠 **CLI 进程内判定**（`exit 2`）；hook 只是分发入口 |
| pre-archive 宿主内真实触发 | 仅 fixture + `host verify` 兜底（**规划中**） |
| catalog 远程在线分发 / npm provenance | **规划中 / 未启用** |

> 〔截图占位 · S-01〕**术语表首页**
> - **画面**：浏览器或编辑器打开仓根 `GLOSSARY.md`，滚动到「简体中文 → 帽制（hat）」小节，可见 `task.md` / `Harness` / `hat` / `kit-*` / 门禁五组术语。
> - **验证点**：读者能一眼看到"帽制"与"门禁"的中英对照。
> - **建议**：窗口宽 1200px，深色主题，字体 14pt。

---

## 2. 安装与版本确认

### 2.1 确认你装的是 3.0.2（已发布）

```bash
# 钉版执行（与 registry latest 一致）
npx spec-wave@3.0.2 --version
```

期望输出：

```
3.0.2
```

> **registry**：`npm view spec-wave version` → `3.0.2`（`dist-tags.latest=3.0.2` · `time.3.0.2`=2026-09-24T00:55:45.386Z）。tag / registry / CLI 三向一致。

> **过渡 bin 仍可用**：`specgate` / `dsh-coding-kit` 是同一入口的旧名，不会报错。但**新脚本请一律写 `npx spec-wave`**。

### 2.2 作为依赖安装

```bash
# 项目内（推荐：钉死版本）
npm i -D spec-wave@3.0.2

# 或零安装试用
npx spec-wave@3.0.2 --help
```

**Node 版本要求**：`^22.19.0 || >=24.0.0`（Node **20 会踩坑** · 见 README Prerequisites）。

**依赖面**：运行时**仅 1 个**依赖（`js-yaml`）。`@deepseek-ai/cordis` 与 `@deepseek-ai/dsh-tools` 是**可选 peer**（只在把本包当 DSH 插件加载时才需要），已在 `peerDependenciesMeta` 标 `optional`。

> 〔截图占位 · S-02〕**版本钉（已发布 · 三向一致）**
> - **画面**：`npx spec-wave@3.0.2 --version` → `3.0.2`；`npm view spec-wave version` → `3.0.2`；tag `v3.0.2` ↔ tip `3d71b90`。
> - **验证点**：工作树钉版、registry latest、CLI 输出均为 3.0.2。
> - **口播**：「装错版本是一切诡异现象的第一嫌疑。先看 `npm view` 与 `--version` 是否同号。」

### 2.3 pnpm 用户的 peer 兜底

若 pnpm 因 peer 链解析失败：

```bash
pnpm add -D spec-wave --config.auto-install-peers=false
# 或仓根 .npmrc 写 auto-install-peers=false
```

---

## 3. 五分钟上手

### 3.1 路径 A · Cursor（最常用）

```bash
cd your-repo
git init                      # 若还不是 git 仓（verify 有 git-root 归属校验）

# ① 只读校验适配表
npx spec-wave@3.0.2 host validate
# 期望：HOST VALIDATE: PASS · exit 0

# ② 先 dry-run 看要写什么（零写入）
npx spec-wave@3.0.2 host apply --tools cursor --profile core

# ③ 确认无误再写盘
npx spec-wave@3.0.2 host apply --tools cursor --profile core --yes
# 期望：HOST APPLY: PASS · exit 0

# ④ 核对物化产物与声明一致
npx spec-wave@3.0.2 host verify --tools cursor
# 期望：HOST VERIFY: PASS
```

写盘后，Cursor 命令面板（⌘/Ctrl+Shift+P）搜 `kit-` 应可见：

```
kit-verify · kit-gate-status · kit-hat-reanchor · kit-init-guide · kit-apply-standards
```

> 〔截图占位 · S-03〕**Cursor 命令面板里的 kit-***
> - **画面**：Cursor 打开 demo 仓，命令面板输入 `kit-`，下拉列出 5 条 `kit-*` 命令。
> - **验证点**：5 条齐全；点开 `kit-verify` 后正文是 **POINTER**（指向 `npx spec-wave verify`），不是 L1 全文。
> - **注意**：`kit-verify` 正文必须让读者看到 **exit 0/1/2 的语义**，禁止口头宣称闸过。

> 〔录屏占位 · R-01〕**host apply 端到端（约 90s）**
> - **镜头 1（0-20s）**：`mktemp -d` 建干净 demo 仓 → `git init` → `pwd` 念出路径（证明不是 kit 源码仓）。
> - **镜头 2（20-45s）**：`host validate` → PASS；`host apply`（无 `--yes`）→ 念出 planned 列表；`ls -a` 证明 `.cursor` 与 `CLAUDE.md` **都还不存在**（dry-run 零写入）。
> - **镜头 3（45-75s）**：`host apply --yes` → PASS；`ls .cursor/commands/` 与 `head CLAUDE.md` 展示真实产物。
> - **镜头 4（75-90s）**：`host verify` → PASS；口播「verify 校验的是物化产物与声明是否一致，篡改会报红 exit 2」。

### 3.2 路径 B · Claude Code

```bash
npx spec-wave@3.0.2 host apply --tools claude --profile core --yes
```

产物：
- `CLAUDE.md`（产品 marker 块 `<!-- cyning-harness:begin -->`）
- `.claude/commands/kit/<verb>.md` → Claude Code 里表现为 **`/kit:verify`** 等
- `.claude/skills/harness-*`
- `.claude/settings.json`（config-hook 落点）

> **local 块永不覆写**：`<!-- cyning-harness-local:begin -->` … `end` 之间的内容，再次 apply 也不会被改动。

> 〔截图占位 · S-04〕**Claude Code slash 列表**
> - **画面**：Claude Code 打开 demo 仓，输入 `/kit:` 触发补全，列出 `/kit:verify`、`/kit:gate-status`、`/kit:hat-reanchor`、`/kit:init-guide`、`/kit:apply-standards`。
> - **验证点**：命名空间是 `/kit:`（不是 `/kit-`）。

### 3.3 路径 C · CI / 非交互（只做过程根，不物化）

```bash
# CI 里必须显式给 --tools（非 TTY 无值 → exit 1）
npx spec-wave@3.0.2 init --preset harness-only --tools none --yes

# 然后跑门禁（CI 的硬停点）
npx spec-wave@3.0.2 verify --task docs/tasks/active/task_<slug>.md
```

> `check` **恒 exit 0**（只给版本建议）；真正的阻断点是 `verify` / `gate-check` / `audit` 的 **exit 2**。

---

## 4. 核心对象

Harness 过程围绕**两个文件级对象**运转。`verify --task` / `gate-check --task` / `task close` 都作用于第一个。

### 4.1 `task.md` —— 一个可执行、可验收的工作单元

**是什么**：一个 Markdown 文件描述一个工作单元（背景/目标 · 范围 · 非范围 · 失败路径 · 验收标准 · Harness 元信息 · 人工闸表）。

**关键约束**：闸表中 `HG-AUDIT-R1` 必须为 `approved`，**帽 30 才可改码**。

**从哪来**：`npx spec-wave sync prompts --yes` 会把 `docs/harness/templates/TASK_TEMPLATE.md` 物化到你的仓。**CLI 永不主动向 `docs/tasks/` 写示例 task**——是否落文件永远由你显式执行。

**放哪**：
- 在途：`docs/tasks/active/task_<slug>.md`
- 归档：`npx spec-wave task close --file <task> --yes` → `docs/tasks/done/`

最小骨架：

```markdown
# Task：加登录限流

> **状态**：`draft`

## Harness 元信息
| 字段 | 值 |
|------|-----|
| **task_slug** | `login-rate-limit` |
| **test_strategy** | `required` |
| **wiki_delta** | `none` |

### 人工闸
| human_gate_id | status | blocks_hats | 说明 |
|---------------|--------|-------------|------|
| HG-AUDIT-R1 | pending | 30 | 22 R1 落盘后人签 |

## 范围 / ## 非范围 / ## 失败路径 / ## 验收标准
（逐节照模板填写；**验收须含可跑命令**）
```

> ⚠️ **闸表必须是 4 列**（末尾要有 `说明` 列）。解析器要求 **4 个单元格**；写成 3 列会被**静默忽略**（闸表解析为空）——此时即使你把 `HG-AUDIT-R1` 填成 `approved`，`gate-check` 仍会阻断，并给出**误导性理由**「HG-AUDIT-R1 非 approved」（状态列渲染为 `?`）。已实测：3 列 + `approved` ⇒ `may_start_30=false`、`gate-check` **rc=2**；补上第 4 列后同内容 ⇒ `may_start_30=true`、**rc=0**。
>
> ⚠️ **另一个会静默失效的写法：`human_gate_id` 单元格中间不要加粗体 `**`**。解析器的 id 捕获组**排除 `*`**，所以：
> - `| **HG-AUDIT-R1** | pending | 30 | 说明 |` → ✅ 能解析（`**` 只是**包裹**整个 id）
> - `| HG-AUDIT-R1（**条件闸**） | pending | 30 | 说明 |` → ❌ **整行失效**（id 格**内嵌** `**`）
>
> 判据：**id 单元格里除了最外层的一对 `**` 之外，不要再出现 `*`**。若闸行莫名"不生效"，先数一下 id 格里有没有多余的 `*`。
>
> **最稳做法：直接复制 `docs/harness/templates/TASK_TEMPLATE.md`**（随包模板已是 4 列）。

### 4.2 `spec.md` —— task 回溯的需求规格

已签的需求规格。task 通过「关联 SPEC」引用它。

```bash
npx spec-wave verify --spec docs/spec/<主题>/<SPEC>.md
# 闸：实现前须已有书面审查
```

放在 `docs/spec/`（本仓按主题分目录）——**CLI 不物化 spec 文件**，由你或你的 Agent 撰写。

### 4.3 帽制（hat system）

| 帽 | 角色 | 干什么 |
|----|------|--------|
| **00** | 委派 | 只分派，不写码 |
| **10** | 规格 / 任务 | 写 `spec.md` / `task.md` |
| **20** | 审查 | 审规格、审任务（产出审查文） |
| **30** | 执行 | **改码**（前置：`HG-AUDIT-R1=approved`） |
| **40** | 自检 | 自查 |
| **50** | 收口 | 归档 |

> **注意**：默认**不物化 30/40** 的 skill（`harness-30-*` / `harness-40-*` 不进默认分发）——执行仍须过 `verify`。

### 4.4 人工闸（human gate）

task 里的 `### 人工闸` 表是 `verify` 的**判定真值**。

| 闸 ID 示例 | 常态 | blocks_hats |
|-----------|------|-------------|
| `HG-AUDIT-R1` | `approved` 后 30 才可改码 | 30 |
| `HG-SCHEMA-CHANGE` | schema/契约变更前的人工裁决 | 依声明 |

**3.0.0 的关键变化（闸判定泛化）**：闸判定不再硬编码 3 个 ID，而是**声明式全闸扫描**——你在闸表里声明的任意 `HG-*` 行都会被读取与判定，`blocks_hats` 列决定它挡哪一顶帽。

> 〔截图占位 · S-05〕**task.md 的人工闸表**
> - **画面**：编辑器打开一个真实 `docs/tasks/active/task_*.md`，高亮 `## Harness 元信息` 与 `### 人工闸` 两节。
> - **验证点**：能看到 `HG-AUDIT-R1 | approved | 30` 一行；旁边 `status` 是 `approved` 而非 `pending`。

---

## 5. 初始化项目骨架

### 5.1 `init`

```bash
npx spec-wave@3.0.2 init --preset harness-only --tools cursor,claude,dsh --yes
```

| 参数 | 语义 |
|------|------|
| `--preset harness-only` | **唯一合法值**（NAME 词表） |
| `--tools all \| none \| LIST` | `LIST` 如 `cursor,claude,dsh`；`all` = 适配表全部；`none` = **只建过程根、不物化** |
| `--profile core \| expanded` | `core` 默认；`expanded` 追加 `kit-hat-*` 薄壳 |
| `--host-adapt` / `--no-host-adapt` | `--no-host-adapt` 既不 apply **也不写粘性** |
| `--target PATH` | 目标仓根 |

**TTY vs 非 TTY**：
- TTY 且无 `--tools` → **交互询问**（多选 / all / none）
- 非 TTY / CI 且无 `--tools` → **exit 1**（必须显式）

### 5.2 `sync prompts`

```bash
npx spec-wave@3.0.2 sync prompts --yes
```

物化 **11 个** prompt 文件 + `docs/harness/templates/TASK_TEMPLATE.md`。

> **默认 dry-run**；本地内容与包内不同 → 列为 `conflict` 且**不覆盖**（`--force` 才覆盖）。

**不覆盖的 S2 过程域**（真值源唯一，`S2_TRUTH_PREFIXES`）：
`docs/tasks/` · `docs/harness/reviews/` · `docs/harness/invokes/by-task/`（以及 legacy 裸 `reviews/` / `invokes/by-task/`）。

### 5.3 目录布局（记住这三个）

```
your-repo/
├── .coding-kit/              # 过程落盘现行根（manifest / host-tools.json 粘性 / backups）
│   └── host-tools.json       #   已选宿主粘性（host_ids + profile）
├── docs/
│   ├── tasks/active|done/    # S2 · task 文件
│   ├── spec/<主题>/          # 需求规格
│   └── harness/
│       ├── templates/        # TASK_TEMPLATE.md
│       ├── prompts/          # 11 个 prompt
│       ├── reviews/          # S2 · 审查文
│       └── invokes/by-task/  # S2 · 留证
└── .cursor/ .claude/ .dsh/   # 宿主物化落点（由 host apply 写）
```

> `.cyning-harness/` 是 **legacy 只读**——勿再当新标准目录。

---

## 6. 多宿主物化

### 6.1 命令四件套

| 命令 | 作用 | 关键语义 |
|------|------|---------|
| `host validate` | 只读校验适配表 | 契约不匹配 → **exit 2 零写入** |
| `host apply` | 写盘物化 | **必须**带 `--tools`；无 `--yes` = dry-run |
| `host update` | 刷新产品 commands/skills | 解析序：CLI `--tools` → 粘性 → 否则 **exit 1**；`conflict` 默认不覆盖（`--force` 才覆盖） |
| `host verify` | 校验物化产物 vs 声明 | 篡改/删除 → 报红 **exit 2**（fail-closed） |

### 6.2 粘性（sticky）

`--yes` 成功写盘后更新 `.coding-kit/host-tools.json`：

```json
{ "version": 1,
  "host_ids": ["cursor", "claude"],
  "profile": "core",
  "updated_at": "2026-09-18T01:23:51.373Z",
  "kit_semver": "3.0.2" }
```

之后升包刷新**不必再抄 `--tools`**：

```bash
npx spec-wave@3.0.2 host update --yes     # 只刷已选宿主
```

> **dry-run 不写粘性**。

### 6.3 13 宿主落点矩阵（profile `core`）

| 宿主 | `host apply` 写入 |
|------|------------------|
| **Cursor** | `.cursor/rules/*.mdc` · `.cursor/commands/kit-*.md` · `.cursor/skills/` · `.cursor/hooks.json` |
| **Claude Code** | `CLAUDE.md` marker 块 · `.claude/commands/kit/<verb>.md`（→ `/kit:verb`）· `.claude/skills/` · `.claude/settings.json` |
| **DSH** | `.dsh/skills/`（帽子技能 + 编排 `kit-*`；**不建** `.dsh/commands/`） |
| **agents** | `AGENTS.md` 片段 · `.agents/skills/` |
| **Copilot** | `AGENTS.md` 片段 · `.github/skills/` |
| **Codex** | `AGENTS.md` 片段 · `.agents/skills/` |
| **Windsurf** | `AGENTS.md` 片段 · `.windsurf/skills/` |
| **Gemini CLI** | `GEMINI.md` · `.gemini/skills/` |
| **opencode** | `AGENTS.md` 片段 · `.agents/skills/` |
| **Roo Code** | `AGENTS.md` 片段 · **不物化 skills**（无官方约定） |
| **Zed** | `AGENTS.md` 片段 · `.agents/skills/` |
| **Cline** | `AGENTS.md` 片段 · `.cline/skills/` |
| **aider** | `AGENTS.md` 片段（**注入层降级**：须 `aider --read AGENTS.md` 或 `.aider.conf.yml` 写 `conventions-file`）· 不物化 skills |

> **永不强造目录**：无官方 skills 目录约定的宿主，就不物化 skills。
>
> **1 个真实观测**：`host apply --tools cursor,claude,dsh --profile core` 的 planned 写入数 = **40 件**（其中 Cursor/Claude 各含 1 个 hook 落点）。

> 〔截图占位 · S-06〕**dry-run 的 planned 列表（强调零写入）**
> - **画面**：终端执行 `host apply --tools cursor,claude,dsh --profile core`（不带 `--yes`），planned 列表滚出 40 行；随后 `ls -a` 显示树里**没有** `.cursor` / `.claude` / `CLAUDE.md`。
> - **验证点**：dry-run 与写盘之间的"零写入"对比是这一段最有说服力的画面。

### 6.4 `host verify` —— 篡改检测

真跑验证（本机实测）：

```bash
# 篡改一个物化文件
echo 'TAMPERED' >> .cursor/skills/harness-10-task/SKILL.md
npx spec-wave@3.0.2 host verify --tools cursor
# → HOST VERIFY: FAIL · exit 2      ← fail-closed 真的咬

# 恢复
npx spec-wave@3.0.2 host update --tools cursor --yes --force
npx spec-wave@3.0.2 host verify --tools cursor
# → HOST VERIFY: PASS · exit 0
```

**比对口径**（依产物类型分档）：
- 普通文件 → **逐字一致**
- `hooks.json` / `settings.json` → **包含性比对**（"用户键不计"，不因你加了自己的 hook 而报红）
- `CLAUDE.md` → **marker 产品块一致**（local 块 / 块外定制不计）

> 〔录屏占位 · R-02〕**篡改 → 报红 → 恢复（约 45s）**
> - **镜头 1**：`host verify` PASS（绿）。
> - **镜头 2**：往某个物化 SKILL.md 追加一行 → 再跑 `host verify` → **FAIL，念出 exit 2**。
> - **镜头 3**：`host update --force` → `host verify` → 复原 PASS。
> - **口播**：「这就是 fail-closed 的实际手感——产物被人改了，包能发现，而且它选择**拦住**而不是放过。」

### 6.5 `host catalog list`

```bash
npx spec-wave@3.0.2 host catalog list          # 内置 + 用户表来源/完整性
npx spec-wave@3.0.2 host catalog list --json
```

**sha256 语义**：**呈现即强制**——列出的摘要即为校验基准。

---

## 7. 门禁链路与 hooks

### 7.1 三层闸（L1 / L2 / L3）

| 层 | 位置 | 是否总可用 |
|----|------|-----------|
| **L1** | prompt 模板（把纪律写进喂给模型的上下文） | ✅ 总可用 |
| **L2** | CI 流水线（`verify` / `gate-check` 的 exit 2 硬停） | ✅ 总可用 |
| **L3** | 宿主运行时 hook（提交/归档时自动触发） | ⚠️ **仅当宿主暴露可钩机制** |

**降到 L1+L2 时必须显式降级并输出可区分**——不允许"静默 no-op 假装有 L3"。

### 7.2 hooks 三态机制

适配表 `surfaces.hooks.mechanism` 三态：

| mechanism | 含义 | 典型宿主 |
|-----------|------|---------|
| `config-hook` | 宿主配置文件里挂钩子 | Cursor（`.cursor/hooks.json`）· Claude Code（`.claude/settings.json`）· Gemini CLI（`.gemini/settings.json`） |
| `shell-hook` | git 层钩子（**宿主中立**） | 任何用 git 的仓 |
| `none` | **无机制 → 显式降级 L1+L2** | 其余宿主（含 DSH 等） |

> ⚠️ **自定义宿主（B5）只能 `mechanism: none`**：`config-hook` 的落点映射表**仅覆盖内置 3 宿主**（`claude` / `cursor` / `gemini` · 真值 `CONFIG_HOOK_HOSTS` · **不扩表**）。非映射表宿主若声明 `config-hook`，`host validate` 会 **PASS + WARN**（stderr / `--json#warnings` · exit 0 不变），但 `host apply` 会 **fail-closed exit 2** 并点名：
> ```
> host apply: config-hook 宿主无物化落点映射: acme-ai（族映射表外宿主 fail-closed · 不静默）
> ```
> 即**自定义宿主只能拿到 L1+L2**（不自动获得 L3）——这是设计边界，非缺陷。想加自定义宿主时请用 `mechanism: none`。

实际物化样例（Cursor）：

```json
{ "version": 1,
  "hooks": { "beforeShellExecution": [
    { "command": "npx spec-wave hook-guard --trigger pre-commit" },
    { "command": "npx spec-wave hook-guard --trigger pre-archive" } ] } }
```

### 7.3 `hook-guard` —— 宿主 hook 的门禁分发

```bash
npx spec-wave hook-guard --trigger pre-commit
npx spec-wave hook-guard --trigger pre-archive
npx spec-wave hook-guard --trigger pre-commit --command 'npx spec-wave@3.0.2 verify --task <task>'
```

**真跑语义**（本机实测）：

| 调用 | 退出码 |
|------|--------|
| `--command 'true'` | **0**（门禁绿 → 放行） |
| `--command 'false'` | **2**（门禁红 → 阻断） |
| 无 `--command`（用默认门禁命令） | 门禁失败即 **2** |

> **实践建议**：默认门禁命令会去调 `npx spec-wave …`。**离线 / 沙箱 CI** 里这会导致取包失败 → fail-closed 阻断（宁拦不放，语义安全）。若你对 CI 的**确定性**有要求，任选其一：
> 1. **预热 npm 缓存**（CI 步骤先 `npm install` / 缓存 `~/.npm`，保证 `npx` 可离线命中）；或
> 2. **物化时钉版**（**实验性 · 缺省关闭** · 不改无旗标默认物化字节）：
>    ```bash
>    npx spec-wave host apply --tools cursor,claude --yes --pin-hook-version
>    # 或显式：--pin-hook-version=3.0.2
>    ```
> 3. **运行时用 `--command` 显式钉版本**：
>    ```bash
>    npx spec-wave hook-guard --trigger pre-commit --command 'npx spec-wave@3.0.2 verify'
>    ```

> 〔截图占位 · S-07〕**hook-guard 红绿对照**
> - **画面**：一张终端并排图 —— 左半 `hook-guard --command 'true'` → 绿色 `rc=0`；右半 `hook-guard --command 'false'` → 红色 `rc=2` + "门禁红 · 阻断 pre-commit"。
> - **验证点**：两个退出码清晰可读。

---

## 8. 校验与门禁命令

### 8.1 退出码契约（**背下来**）

| 码 | 含义 | 命令 |
|----|------|------|
| **0** | 通过 / 仅信息 | `check` **恒为 0** |
| **1** | 用法错误 / 非阻断失败 | 缺必填旗标、未知参数 |
| **2** | **门禁阻断（failClosed）不得放行** | `verify` / `gate-check` / `audit` |

> **禁止**在本地把 2 改映射成 0 来"继续跑"。

### 8.2 `verify` 三种模式

```bash
# ① task 模式（30 前闸）
npx spec-wave verify --task docs/tasks/active/task_<slug>.md

# ② spec 模式（SPEC→00 前审查存在性闸）
npx spec-wave verify --spec docs/spec/<主题>/<SPEC>.md

# ③ 裸模式（2.3 起）：仓级 reviews 双路径全量扫描
npx spec-wave verify
#   done → failClosed；active → 仅信息报告
#   存量豁免：docs/harness/legacy-gate-exempt.yaml
```

**`--with-wiki-lint`**（显式旗标，非破坏）：在既有检查之上**追加** wiki-delta lint。

```bash
npx spec-wave verify --task <task> --with-wiki-lint
```

有缺口 → BLOCKED，并打印**与 PR CI 逐字一致**的复跑命令：
```bash
npx --yes spec-wave task lint-wiki-delta --target .
```

### 8.3 D5 测试制品探测（`test_strategy=required` 时）

`audit` / `verify` 会检查仓内是否有**真实测试制品**，否则 **exit 2**。**注意：D5 是制品探测，不执行测试命令。**

强信号探针：目录 `test/` `tests/` `spec/` `specs/` `__tests__/`；配置 `jest/vitest/playwright/cypress.config.*` · `pytest.ini`；文件名（仓根 3 层内）`*.{test,spec}.{js,ts,mjs,cjs}` · `*_test.py` · `test_*.py`；以及 CI workflow 里的 test 步骤。

**误判面提醒**：`pyproject.toml` / `setup.py` **不算**（任意现代 Python 仓都有）；纯 lint / 纯部署 workflow 不放行；探测深度 3 层——monorepo 更深层请补一个 `tests/` 目录。

### 8.4 `gate-check` / `status` / `audit`

```bash
npx spec-wave gate-check --task <task>       # 只看闸
npx spec-wave status --check                 # 状态 + 是否被封
npx spec-wave status --task <task> --json
npx spec-wave audit --task <task>            # 审计判定（exit 2 同族）
```

### 8.5 `check` —— 版本三向比较

```bash
npx spec-wave check
```

对 `manifest.version` 与包版本做三向比较：「已是最新 / 可升级 / 高于」。**恒 exit 0**。

> 〔截图占位 · S-08〕**verify BLOCKED 的画面**
> - **画面**：一个 `HG-AUDIT-R1` 仍为 `pending` 的 task，跑 `verify --task` → 输出 BLOCKED 与原因，终端提示 `exit 2`。
> - **验证点**：阻断原因直指人工闸未批，而非含糊报错。

---

## 9. 任务生命周期

```bash
# 单文件校验
npx spec-wave task lint --file <task.md>
npx spec-wave task check --file <task.md>

# 收口归档（验收后）
npx spec-wave task close --file <task.md> --yes
#   → docs/tasks/active/… 移动至 docs/tasks/done/

# 批量
npx spec-wave task lint-done            # 扫 done 目录
npx spec-wave task lint-wiki-delta      # wiki_delta 口径
npx spec-wave status --check
```

**`task lint-wiki-delta` 诊断码**：

| 码 | 含义 |
|----|------|
| `wiki_delta_missing` | 缺字段 |
| `wiki_delta_wrong_section` | 字段写在 `## Harness 元信息` **之外**的节（替代 missing，不双报） |
| `wiki_delta_invalid`（仅 `--strict`） | 值非法 |
| `wiki_delta_path_missing`（仅 `--strict`） | 声明的路径不存在 |

### 9.1 `timeline` / `lifecycle` / `discipline`

```bash
npx spec-wave timeline --task <task> [--limit N] [--ingest]
npx spec-wave lifecycle show --json
npx spec-wave lifecycle dry-run --transition <ID> --from <STATE>
npx spec-wave discipline show --json
```

> 〔截图占位 · S-09〕**task close 前后目录对比**
> - **画面**：左：`ls docs/tasks/active/` 有 `task_xxx.md`；右：`task close --file … --yes` 后，`active/` 空了、`done/` 出现了该文件。
> - **验证点**：归档是**移动**，不是复制。

---

## 10. 图谱与本体

### 10.1 图命令

```bash
npx spec-wave graph yaml compile
npx spec-wave graph yaml check  [--all]
npx spec-wave graph yaml export

npx spec-wave graph ingest
npx spec-wave graph snapshot
npx spec-wave graph axioms

npx spec-wave graph ontology check [--file PATH] [--json]
npx spec-wave graph ontology check --hgm        # 事件轨图谱实例 ⊆ 随包本体词汇
```

### 10.2 ⚠️ 本体边界（重要 · 3.0 ONTO-OPEN 裁决）

| 项 | 状态 |
|----|------|
| 图能力（`graph yaml compile/check/export`、消费者自建图） | ✅ **已开放** |
| `graph ontology check [--file PATH]` 校验器 | ✅ 已开放（可校验随包本体或你指定的漂移副本） |
| **自定义本体**（自定义类 / 关系） | ❌ **不开放** |

随包 `assets/ontology.yaml` 是 SpecWave **自用元模型**。**校验器开放 ≠ 本体内容开放。**

复议触发条件：真实消费者请求 · ontology-check 面稳定一个 minor 后重估 · B5 后生态拉取 —— 走 `HG-SCHEMA-CHANGE` 式人闸。

### 10.3 `wiki export`

```bash
npx spec-wave wiki export --json [--backlinks|--no-backlinks] [--incremental] [--check-conflicts]
```

F3 三能力：`backlinks`（双向链接）· `--incremental`（增量等价）· `--check-conflicts`（冲突检测）。

---

## 11. 审计与可观测

```bash
# 审计事件轨（W6）
npx spec-wave audit --task <task>

# 纪律收窄检查
npx spec-wave discipline show

# pin 一致性（release-pins 是发布纪律的真值源）
npx spec-wave pins check [--json]
npx spec-wave pins fix [--yes]          # 默认 dry-run · S2 机械拒写

# 资产完整性
npx spec-wave assets verify [--json]     # sha256 · 偏差 exit 2
npx spec-wave assets manifest rebuild [--yes]   # 默认 dry-run；修复对象=manifest，资产永不反向改
```

**三个机检脚本**（维护者/CI）：

```bash
node scripts/check-claims.mjs        # A3 对外口径黑名单 · forbidden_wording 零命中
node scripts/check-terminology.mjs   # 术语机检（terminology.yaml）
node scripts/check-doc-links.mjs     # 文档链接两级机检
```

---

## 12. DSH 插件面（可选）

```bash
dsh plugin --profile web add spec-wave
dsh --profile web --dump-config       # 确认层：bundles 含 spec-wave
```

> **`dsh-coding-kit` 已 deprecate**——DSH 插件请装正式包名 `spec-wave`。

### 12.1 激活与调用

1. 用该 profile 启动 DSH
2. 对话中说：**请应用 coding standards**
3. 模型调用工具 `apply_coding_standards`
4. 成功后后续回合的 runtime context 含 `# Coding Standards`

> ⚠️ **加载 ≠ 注入**。安装插件**不会**自动改写 system prompt；`apply()` 只注册工具，必须显式调用 `apply_coding_standards`。

**profile 档**：`l1`（L1 规范 + coding_wiki）· `l1+l2`（默认，全部 standards）· `full`（当前等价 `l1+l2`，为后续扩展预留）。

**override 根查找**：从 cwd 逐级向上探测 `.coding-kit` / `.dsh/coding-kit`，在**最近的含 `.git` 的祖先**（git root）截止。

**截断**：注入超 24k 字符按**文件边界**截断（不会注入半份文件），附 `truncated=true`。

### 12.2 读写根不对称（易踩）

| 面 | 行为 |
|----|------|
| **读取面** `apply_coding_standards` | 向上查找到 **git root** |
| **写入面** `init_coding_kit` | 写入**当前工作目录** |

→ 请在**仓根**对话里调 `init_coding_kit`，避免 monorepo 子目录初始化后读取面却命中仓根。

---

## 13. 升级与迁移

### 13.1 从 `@cyning/harness` → `spec-wave@3.0.2`（最小路径 · 顺序固定）

```bash
# ① 换依赖
#   devDependencies: @cyning/harness → spec-wave@3.0.2

# ② 仓根执行 upgrade
npx spec-wave upgrade --yes
#   读 → .coding-kit/manifest.json，否则 legacy .cyning-harness/manifest.json
#   写 → .coding-kit/manifest.json（version 钉 3.0.0，from_version 记旧号）
#   不删除 .cyning-harness/

# ③ CI / 脚本里所有 npx @cyning/harness 与 npx dsh-coding-kit → npx spec-wave
```

**布局**：过程落盘现行根为 **`.coding-kit/`**；`.cyning-harness/` 为 **legacy 只读**。

### 13.2 2.x → 3.0.0（host-adapt 适配表 schema v1 → v2）

> **这是 3.0.0 唯一的 Breaking 项，但它对绝大多数人是"什么都不用做"。**

#### ① 默认路径：什么都不用做

- **`--file` 用旧格式适配表的消费者**：**零改动**。无 `schema_version` 键即按 v1 旧扁平语义解析（缺省 = v1）。
- **仅用内置 13 宿主的消费者**：`host validate/apply/update` 的**既有落点不变**；内置表已升级为 v2，因此 `host apply` 会**额外新增 2 件 hooks 物化**（`.cursor/hooks.json` / `.claude/settings.json`）——这是 W2 的 **additive 新能力**，不是破坏。

> **本手册的独立实测保证**（对照实验，2026-09-18）：
> 用 **v2.4.1** 与 **3.0.0 + v2.4.1 旧表** 分别物化到两个干净仓，再递归 diff：
> **全部 38 件宿主物化文件字节级相同**，唯一差异是 `.coding-kit/host-tools.json` 里的 `updated_at` 时间戳与 `kit_semver` 版本戳。
> ⇒ 「v1 表面逐字锁 · 旧表零改动」**经对照实验证实**，非自述。

#### ② 自定义表作者：想用新能力时（可选迁移）

1. 表首加 `schema_version: 2`（**整数**，不复用 `version` 字符串字段）
2. **可选**：把重复的 `verify` / `skills` 提入根级 `defaults`，或用行级 `extends`（合并语义：标量子覆盖父 · 对象逐键深合并 · **数组整体替换**）
3. **必做**：声明根级 `command_sets`（v2 表缺此节 **fail-closed**；`core`/`expanded` 非空数组）
4. **可选**：声明 `surfaces.hooks`（`mechanism` + `triggers` + `command`）

#### ③ 禁止事项

- 不要手写 `schema_version` > 2（**fail-closed 拒**，点名「未知 schema_version」，不会静默按旧格式解析）
- 不要在 `command_sets.core`/`expanded` 含 `kit-30` / `kit-publish`（内建禁词机检拒）

#### ④ `refresh-ide-blocks`（存量 IDE 块旧命令刷写）

旧包时代嵌在 marker 块里的旧命令字面可以机械刷写：

```bash
npx spec-wave refresh-ide-blocks --dry-run   # 默认即 dry-run
npx spec-wave refresh-ide-blocks --yes       # 才写盘
```

- **发现面（冻结白名单）**：仓根 `AGENTS.md` · `CLAUDE.md` · `.cursor/rules/*.mdc`（单层）
- **preflight（`--yes` 专用）**：git 脏树（**untracked 也算脏**）/ 单文件新旧混杂 / marker 配对畸形 → **exit 2 零写入**
- **备份**：`.coding-kit/backups/refresh-ide-blocks/<UTCts>/`（保留最近 5 代）
- **幂等**：刷过再跑命中 0，字节不变，exit 0

> **给存量仓 Agent 的可复制 Prompt**：见 `README.zh-CN.md`「可复制 Prompt（给存量仓 Agent）」节。

> 〔录屏占位 · R-03〕**2.x 仓升级到 3.0.0（约 120s）**
> - **镜头 1（0-30s）**：`git worktree add` 检出 v2.4.1 → 显示旧适配表**没有** `schema_version` 键。
> - **镜头 2（30-60s）**：用 3.0.0 的 CLI 对**同一张旧表**跑 `host validate --file` → **PASS**（念：旧表零改动可读）。
> - **镜头 3（60-100s）**：分别物化到两个干净仓 → `diff -r` → 唯一差异是 `host-tools.json` 的版本戳。
> - **镜头 4（100-120s）**：`host update --yes` 刷粘性 → `host verify` → PASS。
> - **口播**：「升级的痛点被压到几乎为零——旧表原样跑，想用新能力再加一行 `schema_version: 2`。」

---

## 14. 排障 FAQ

| # | 症状 | 原因 / 处置 |
|---|------|------------|
| 1 | `host update` 报 **exit 1** | 缺 `--tools` **且**无粘性。先 `host apply --tools … --yes` 建粘性 |
| 2 | `host apply` 报用法错误 | `host apply` **必须**带 `--tools`（`init` 才可省） |
| 3 | `verify` 报 BLOCKED | 看是否 `HG-AUDIT-R1` 仍 `pending`；帽 30 前必须 `approved` |
| 4 | D5 报「无测试制品」exit 2 | 仓内补强信号：`tests/` 目录、`*.test.ts`、或含 test 步骤的 CI |
| 5 | `host verify` 报红 | 产物被改/删。`host update --tools … --yes --force` 恢复 |
| 6 | hooks 没拦住提交 | 该宿主 `mechanism` 可能是 `none`（**显式降级 L1+L2** 是设计，不是 bug）。查 `host catalog list` |
| 7 | CI 里 hook 报门禁红但本地绿 | 默认门禁命令取 `npx spec-wave` 需联网；用 `--command` 钉版 + 预热缓存 |
| 8 | Cursor 看不到 `kit-*` | 命令是**项目级**：确认在仓根 apply，且 Cursor 打开的是该项目根 |
| 9 | `CLAUDE.md` 里我加的东西没了 | 你的内容应在 `<!-- cyning-harness-local:begin -->` 块内（apply 不覆写 local 块） |
| 10 | 想直接用旧版 | `npx spec-wave@2.4.2 …`；但 3.0.0 对旧表**零改动兼容**，没有停在 2.x 的必要 |
| 11 | pnpm 装不上 | `auto-install-peers=false` |
| 12 | IDE 报 `cordis.patch.yml` 缺 `op`/`path` | **误报**（yaml-language-server 把它当 RFC6902）。该文件必须保持 `- insert`，勿改 |
| 13 | 用 `--file` 自定义表 `apply` 后，不带 `--file` 跑 `host verify` 报 `hooks.json`/`settings.json` **missing** → exit 2 | **已知 3.0.0 兼容性误报**（粘性不记表源）。**处置：`verify` 与 `apply` 带同一 `--file`**；或重新 `apply`（内置表路径）。未漏检真实篡改 |

> ⚠️ **FAQ #13 详解**（`--file` 用户必读）：`host apply --file <表>` 之后，`host verify` **必须带同一 `--file`**。否则 verify 缺省走**内置 v2 表**，会期望 2 件 hooks 落点（`.cursor/hooks.json` / `.claude/settings.json`），而你的旧/自定义表**本就不会物化**它们 ⇒ 得到**误报 exit 2**。
> ```bash
> # 正确：三处带同一张表
> npx spec-wave host validate --file my-hosts.yaml
> npx spec-wave host apply --tools cursor --file my-hosts.yaml --yes
> npx spec-wave host verify --tools cursor --file my-hosts.yaml     # ← 别漏
> ```

> 〔截图占位 · S-10〕**FAQ 场景实测截图集**
> - **画面**：6 张小截图拼版，分别对应 FAQ #1 / #3 / #5 / #6 / #8 / #12 的实际报错输出。
> - **验证点**：每张图的报错文案与 FAQ 表描述一致。

---

## 附 A · 命令全表（速查）

```text
# —— 版本 / 帮助 ——
npx spec-wave --version | -V
npx spec-wave --help | -h

# —— 初始化 / 升级 ——
npx spec-wave init [--preset harness-only] [--tools all|none|LIST] [--profile core|expanded] [--host-adapt|--no-host-adapt] [--target PATH] [--yes]
npx spec-wave upgrade [--target PATH] [--yes]
npx spec-wave refresh-ide-blocks [--target PATH] [--dry-run] [--yes] [--json]

# —— 门禁 / 校验 ——
npx spec-wave check [--target PATH]
npx spec-wave verify [--target PATH] [--task FILE | --spec FILE] [--json] [--with-wiki-lint]
npx spec-wave gate-check [--target PATH] [--task FILE] [--json]
npx spec-wave audit [--target PATH] [--task FILE]

# —— 任务 ——
npx spec-wave task lint --file PATH
npx spec-wave task check --file PATH
npx spec-wave task close --file PATH [--yes] [--json]
npx spec-wave task lint-done [--target PATH]
npx spec-wave task lint-wiki-delta [--target PATH] [--scope all|active|done] [--strict] [--json]

# —— 状态 / 时间线 / 生命周期 ——
npx spec-wave status [--target PATH] [--task FILE] [--json] [--check]
npx spec-wave timeline --task FILE [--target PATH] [--json] [--limit N] [--ingest]
npx spec-wave lifecycle show [--json]
npx spec-wave lifecycle dry-run --transition ID --from STATE [--task PATH] [--target PATH]
npx spec-wave discipline show [--json]

# —— 图谱 / 本体 / wiki ——
npx spec-wave graph yaml compile|check|export …
npx spec-wave graph ingest|snapshot|axioms …
npx spec-wave graph ontology check [--file PATH] [--json] [--hgm]
npx spec-wave wiki export --json [--target PATH] [--root DIR] [--out FILE|-] [--backlinks|--no-backlinks] [--incremental] [--check-conflicts]

# —— 同步 / 技能 ——
npx spec-wave sync index [--target PATH]
npx spec-wave sync prompts [--target PATH] [--yes] [--force] [--json]
npx spec-wave skills install [--target DIR] [--out DIR] [--global] [--force] [--with-execute-hats]
npx spec-wave skills build [--with-execute-hats]
npx spec-wave skills check

# —— 宿主物化 ——
npx spec-wave host validate [--file PATH] [--json]
npx spec-wave host apply --tools LIST [--profile core] [--target PATH] [--file PATH] [--json] [--dry-run|--yes]
npx spec-wave host update [--tools LIST] [--profile core] [--target PATH] [--file PATH] [--json] [--dry-run|--yes] [--force]
npx spec-wave host verify [--tools LIST|all] [--profile core|expanded] [--target PATH] [--file PATH] [--json]
npx spec-wave host catalog list [--target PATH] [--json]
npx spec-wave hook-guard --trigger pre-commit|pre-archive [--command GATE_CMD] [--target PATH]

# —— 发布纪律 ——
npx spec-wave pins check [--target PATH] [--json]
npx spec-wave pins fix [--target PATH] [--yes]
npx spec-wave assets verify [--target PATH] [--json]
npx spec-wave assets manifest rebuild [--target PATH] [--yes]

# —— 退出码 ——
# 0 = pass / 仅信息（check 恒 0）
# 1 = 用法错误 / 非阻断失败
# 2 = 门禁 BLOCKED（failClosed · verify / gate-check / audit / D5）
```

---

## 附 B · 截图 / 录屏占位清单（待补素材）

> 本表是**待补媒体素材的索引**。每一条都可在正文中按编号检索到对应描述块。补录后请把对应块替换为真实媒体（建议：截图 PNG ≤ 400KB，录屏 MP4/GIF ≤ 8MB）。

| 编号 | 类型 | 标题 | 所在章节 | 建议时长/尺寸 | 状态 |
|------|------|------|---------|--------------|------|
| **S-01** | 截图 | 术语表首页（帽制小节） | §1.2 | 1200px · 深色 | ⬜ 待补 |
| **S-02** | 截图 | 版本三向一致 | §2.1 | 终端整屏 | ⬜ 待补 |
| **S-03** | 截图 | Cursor 命令面板里的 `kit-*` | §3.1 | Cursor GUI 整窗 | ⬜ 待补 |
| **S-04** | 截图 | Claude Code `/kit:` slash 列表 | §3.2 | Claude Code GUI | ⬜ 待补 |
| **S-05** | 截图 | `task.md` 的人工闸表 | §4.4 | 编辑器窗口 | ⬜ 待补 |
| **S-06** | 截图 | dry-run planned 列表 + 零写入对比 | §6.3 | 终端整屏 | ⬜ 待补 |
| **S-07** | 截图 | `hook-guard` 红绿对照 | §7.3 | 终端并排 | ⬜ 待补 |
| **S-08** | 截图 | `verify` BLOCKED 画面 | §8.5 | 终端整屏 | ⬜ 待补 |
| **S-09** | 截图 | `task close` 前后目录对比 | §9.1 | 终端并排 | ⬜ 待补 |
| **S-10** | 截图集 | FAQ 场景实测（6 张拼版） | §14 | 拼版 1600px | ⬜ 待补 |
| **R-01** | 录屏 | `host apply` 端到端 | §3.1 | 约 90s | ⬜ 待补 |
| **R-02** | 录屏 | 篡改 → 报红 → 恢复 | §6.4 | 约 45s | ⬜ 待补 |
| **R-03** | 录屏 | 2.x 仓升级到 3.0.0 | §13.2 | 约 120s | ⬜ 待补 |

**补录规范建议**：
- 终端统一：深色主题 · 字号 ≥ 14pt · 路径脱敏（勿露真实用户名/内网地址）
- 录屏统一：先 `mktemp -d` 建干净 demo 仓（**不要在 SpecWave 源码仓里录**）
- 所有录屏建议带**退出码特写**（`echo rc=$?`），因为"闸真咬"是本产品最核心的说服点

---

## 修订

| 日期 | 摘要 |
|------|------|
| 2026-09-18 | 初版 · 配合 `spec-wave@3.0.1` 发布（tag `v3.0.0`） |
| 2026-09-23 | 头栏与文内现行钉随 `spec-wave@3.0.2` 待发版同步（tag 由 00 按授权代打 · publish 待人 · registry `latest` 仍 `3.0.1` · 3.0.1 回填已发布） |
| 2026-09-24 | 头栏 / 章 2 回填 **已发布**（registry `latest=3.0.2` · `time.3.0.2`=2026-09-24T00:55:45.386Z · tag tip `3d71b90` · task `3-0-2-postpublish-dx`） |
