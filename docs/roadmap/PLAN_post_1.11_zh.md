# 规划 · 1.11.0 之后 · 1.12 / 2.0

> **状态**：`approved`（**HG-NEXT-PLAN** · 2026-09-10 维护者「先 1.12」）  
> **基线**：`dsh-coding-kit@1.11.0` · PR #30 · 验收报告 `.workbuddy/output/验收报告-1.11.0-F1-F5.md`  
> **拟发版**：`dsh-coding-kit@1.12.0`  
> **上游纪律**：`delivery/高层架构设计.md` §4.3 / §6 —— **MVP ⊂ 完整版**；F6 须建立在 F1–F5 真值源之上（已满足）  
> **系列 SPEC**：[`docs/spec/1x-closeout/`](../spec/1x-closeout/)  
> **Open Folder**：`dsh-coding-kit/`  
> **本文件不做**：改码 · npm publish · 填 EOS 日（仅人）

---

## 一句话

1.11 已把内部一致性（F1–F5）做完；**下一可发版本已定为 `1.12.0`（1.x 收口）**，再开 **`2.0.0`（F6 宿主适配表）**——禁止跳过收口直接铺跨宿主。

---

## 版本切分（推荐）

| 版本 | 定位 | 是否 major |
|------|------|------------|
| **1.12.0** | 1.x **收口**：F3 T1 deprecate、工程债、本体论浅落地、F6 **预备**（声明清单草稿） | minor |
| **2.0.0** | **完整版**：F6 宿主适配表落地；DSH = 首个宿主（非唯一） | major |

**弃选**：把 F6 整包塞进 1.12（破坏「适配表 = 2.0」叙事与 semver 预期）。

---

## 1.12.0 · 候选范围（按优先级）

### P0 · 验收遗留 / 治理闭环

| ID | 内容 | 来源 | 备注 |
|----|------|------|------|
| **C1** | 拍板 **`HG-EOS-DATE`** + 公开 EOS / 新注册截止 + **人**执行 `npm deprecate @cyning/harness` | 验收 §8.2 · F3 T1 | **仅人**；Agent 只改 `MIGRATION.md` 日历与文案 |
| **C2** | 全量 `npm test` / CI 稳定性（验收沙箱 SIGKILL 遗留） | 验收 §8.4 · §6 备注 | 不扩产品面；先证伪是否 runner OOM / 超时 |
| **C3** | 消费者仓抽样：`npx dsh-coding-kit@1.11.0 verify/check` 端到端（exit 契约） | 验收 §8.1 | dogfood；失败则开缺陷 task |

### P1 · 本体论实践（对齐 F5=B）

| ID | 内容 | 说明 |
|----|------|------|
| **O1** | `ontology.yaml` **消费面**最小闭环 | 现状：`product_semver` 钉测已有；`ontology-check` 仍「未接线」。1.12 可选：只读校验脚本 / `graph axioms` 旁路文档化，**或** 新子命令草案（须独立 SPEC） |
| **O2** | 从 ontology 投影「现行能力一页」 | 避免再养散文 SPEC 当版本钉（守 B）；可由 CI 生成 `docs/` 或 README 指针段 |
| **O3** | `discipline-coverage` 与 lifecycle「已接线」口径对齐 | 历史 X4 类债；收窄误读 |

### P2 · F6 预备（不落地适配引擎）

| ID | 内容 | 说明 |
|----|------|------|
| **P6-prep** | **宿主落点清单（声明式草稿）** | 盘点现有 `assets/ide/adapters/`、`AGENTS.md` / `CLAUDE.md` / `.cursor/rules` 写入面；输出 YAML/表，**不**做多宿主编译器 |
| **P6-contract** | DSH 契约版本嗅探需求条文 | 写入 2.0 SPEC 输入；1.12 只成文不实现亦可 |

### P3 · 文档债（可裁）

| ID | 内容 |
|----|------|
| **D1** | `docs/releases/` 续写至 1.11.0（验收 X3 类时效） |
| **D2** | `delivery/promotion` 路线图改「1.x MVP = **DONE**」+ 链到本文件 |

### 1.12 明确非范围

- F6 多宿主编译 / Ruler 式 `apply` 全表  
- 自研 IDE · 商业化 · 第二分发通道 · 云策略引擎  
- 删除消费者 `.cyning-harness/`  
- 把分层强制做成远程 Policy 引擎（F2 已冻结文档-only）

---

## 2.0.0 · 完整版（在 1.12 收口后）

| ID | 内容 | 验收草案 |
|----|------|----------|
| **F6** | 宿主适配表：单一规范源 → N 宿主原生配置位；默认落点 **`AGENTS.md`** | ≥2 个非 DSH 宿主可物化；DSH 仍为首个 |
| **U-01** | DSH 契约版本嗅探 + 不匹配降级 | 错版本不静默写坏 |
| **V5**（可选同波或紧随） | 重复常量 / 测试口径单一化 | 架构 V5 |

**开工闸**：1.12 P0（至少 C1 日历成文；deprecate 实操可与 2.0 并行但须人闸）· 独立 `docs/spec/2x-host-adapt/`（或等价）整包签收。

---

## 建议 Wave（1.12）

| Wave | 覆盖 | 人闸 |
|------|------|------|
| **W0** | 本规划签收 · 拆 task slug | `HG-NEXT-PLAN`（本文件） |
| **W1** | C2 CI/全量测稳 + C3 dogfood 纪要 | 无 / 轻 |
| **W2** | C1 EOS 日历成文 → **人** deprecate | **`HG-EOS-DATE`** |
| **W3** | O1–O3 本体论浅落地（选型后） | 若改 CLI 则 `HG-AUDIT-R1` |
| **W4** | P6-prep 声明清单 + D1/D2 文档 | 无码或低码 |

发版：Agent bump → 人 `npm publish`（RELEASING）。

---

## 与验收报告 §8 映射

| 验收建议 | 落入 |
|----------|------|
| 端到端 check/verify | C3 |
| `HG-EOS-DATE` + deprecate | C1 / W2 |
| 1.12 路线对齐 promotion | 本文件 + D2 |
| 全量 `npm test` / CI | C2 |

---

## 维护者拍板（2026-09-10）

| # | 问题 | 决议 |
|---|------|------|
| 1 | 下一发版号 | **1.12.0**（用户「先 1.12」· **不开** 2.0 本波） |
| 2 | deprecate 实操 | **采纳建议**：日历必达；deprecate **争取同波**（仍仅人 · 须 `HG-EOS-DATE`） |
| 3 | O1 深度 | **采纳建议**：先脚本/测；新 CLI 另开 SPEC |
| 4 | F6 预备 | **采纳建议**：进 1.12，仅 P6-prep 声明清单 |

**`HG-NEXT-PLAN`**：**approved（1.12 路径）** · 2026-09-10  
**2.0 / F6**：本波 **拒开工**；等 1.12 收口后再签独立 SPEC。

---

## 开放拍板（维护者）

| # | 问题 | 状态 |
|---|------|------|
| 1–4 | 见上表 | **已拍** |
| 5 | `HG-EOS-DATE` 具体日历日 | **仍 pending**（阻塞 deprecate 实操，不阻塞 W1 CI/dogfood） |

---

## 修订记录

| 日期 | 摘要 |
|------|------|
| 2026-09-10 | 初稿：验收通过后规划 1.12 收口 + 2.0 F6 |
| 2026-09-10 | **拍板先 1.12**；开放项收敛为 EOS 日历；挂 `docs/spec/1x-closeout/` |